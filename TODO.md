# CLIMBmix — Progress & Remaining Work

## Completed

- **db≥2 内存墙判决 + db 梯子作废 + 性能路标（2026-09-17 下午；守卫 rev3 落地。scope = d28 ~2.4B 参数 / 29.49G 卡 / fp32-master 代码形态，非普适结论）**: ① **事故链**: 13:01 双臂 `MID_DEVICE_BATCH_SIZE=4`@ws=64 → step-0 首 forward OOM 27.84G（gpt.py:95）；14:45 db=2 → 同点 27.61G（18MiB 申请被拒）。rev2 守卫双双放行（GA=2/4 整除）——**身份合法 ≠ 内存合法**：守卫挡得住预算错误，挡不住未验证的内存点。16:0x db=1 → 两臂（climb 418f1301 / random3b e2fae0c0）训练正常（dt 3.35s/步、mfu 14.4，与 09-16 f1bad8c9 逐位一致）。② **根因不是新发现——08-28 已判且入档**（"Production dbs decision (FINAL)"，dbs≥2 forward wall 26.9-27.5G@ws=8）: 每卡静态 ~15G = fp32 master 7.38（08-27 服务器逐张量实测：182 矩阵张量全 fp32 + bf16 嵌入 + fp32 lm_head）+ fp32 grads ~7.37 + 分片动量（ws≥64 时极小），**ws 与 db 均无关**；前向随 db 线性（db=1 ≈6.4G、db=2 ≈12.8G）→ db=2 ≈27.8G 顶穿 29.49G。死亡点报的是分配器天花板不是需求量，故 db=2/4 同报 ~27.5-27.8。**计算全程 bf16、存储 fp32 master 是混合精度标准做法**（common.py:20 / gpt.py:50 逐算子 cast；bf16 尾数 8 位，小 lr 更新会被舍入吞掉；隐性代价 = fp32 梯度 all_reduce 通信字节 ×2）。③ **错误来源 = db 梯子幻觉**: prod3 "4 节点 db=8" 被当可外推经验点（该锚点算术不可能、已降级待核，见 #10 勘误），rev1 守卫的 node-count-mismatch 语义又强化了"每节点数有标准 db"的心智模型。④ **全库实证表（修正版）**: db=1 ✓✓✓ ws=8（08-28 speedrun）/ ws=64（09-16 f1bad8c9 至 78%、09-17 两臂）/ ws=128（09-16 2e58bdab + d9b8dae3 **训满 2861 步**，随后死于 eval——53d0bcd/20b94b0 之前的代码，ckpt 因无 salvage 丢失，bc2cdcf 因此而生）；db=2 ✗✗ ws=8（08-28, 26.9G）/ ws=64（09-17, 27.61G）；db=4 ✗✗ ws=8（27.5G）/ ws=64（27.84G）。**db≥2 从未在任何 ws 成功；墙 ws 无关**——昨日疑似反例 "16 节点 micro=2" 经 2e58bdab 启动命令实锤为 micro=1（#10/#11 锚点已勘误）。⑤ **规则（防再犯）**: MID_DEVICE_BATCH_SIZE 固定默认 1（事故全部来自显式覆盖）；**守卫 rev3（本 commit）**: db≥2 需显式 `MID_DEVICE_BATCH_OK=1`（新分支在 db=1 时不可达，默认发射零影响）；优化器/dtype/内存路径的代码改动使实证表作废；新 (ws,db) 组合从最低已验证 db 起步；db≥2 的唯一合法通道 = master bf16 化（省 ~5.5G/卡，db=2 → ~22G 可活）+ 专项验证，20B 轮设计时评估。⑥ **性能路标（"db=1 之后还有没有空间"的答案；执行挂 #11 的测量先行程序）**: (a) 墙钟-节点数（已验证）: 16 节点 db=1 = 2.12s/步（111min/臂）vs 8 节点 3.35s（160min/臂）= **1.58×，代价 +39% node-minutes**；8→16 加速比 1.58 < 1.8 预登记判据 → **32 节点现配方判死（#10 判决生效）**；L1a 赶墙钟可选 16 节点。(b) **优化器通信合并（#11 主战场）**: ~570 逐参数 all_reduce + ~570 broadcast/步 → 同形状组 flat-concat ~40 次，mfu 14% 的主要回收空间，修完大 ws 加速比同步改善。(c) master bf16 化 = db≥2 钥匙，但每 rank 每步总工作量不变（GA 补偿）、收益仅 kernel 摊销（估 10-30%）+ 优化精度风险——20B 轮再议。(d) total_batch 重设计（#10，20B 轮，2²² 方案 + critical batch size 联动）。（两臂落地后补 (ws=64, db=1) 峰值内存实测进本条，当前按静态模型推 ~21-23G。）
- **prod4 三作业 10:30 同时阵亡判读 + 双修复（2026-09-17 上午；排队 13-15h 后同时起跑、入轨 ~10 分钟内全灭，三种表象两个根因）**: ① **双臂（climb8/random3b8）= boot shell 硬编码 mixture_data × 内容键化改名**：85e8321 把臂 mixture 上传路径改为 `mixture_data_k{hash}` 并写入 spec.mixture_data_uri，但 climbmix-ma 的 boot shell（另一仓库，85e8321 未触及）仍只链接 `EXP_IN/{spec.json, mixture_data, result}` 三个硬编码名字 → 键化目录不在 obs view 里 → worker `LocalStorage.list_objects` 对未链接路径静默返回 [] → `download_dir` 建空目录 → 8 rank 在 dataloader.py:53 断言阵亡（~10 分钟、零信息量）。搜索舰队不受影响（exps/exp_XXXX/mixture_data 从未键化）。OBS 侧数据完好（submit 视图 stat 668/668 + 569/569 全在；legacy 无键目录也在——路径迁移痕迹，成孤儿且让每节点输入挂载翻倍到 ~18G，与 6B 孤儿一批清理）。**修复一**：climbmix-ma `146f465` boot shell 链接全部 staged 子项（result 仍映射输出挂载）+ 回归测试 195/195（键化目录进 view + 旧名字兼容）；**修复二**：remote_worker mixture 下载后 fail-fast（无 *.parquet 即拒发训练——镜像 2026-09-12 eval_only 同款守卫，prod2_runtime 全绿）。**两修都对"新提交"生效**：boot shell 在 submit 时由 submit 侧 climbmix-ma 生成、worker 资产随下次 dispatch 自动重发布，无需容器侧动作。② **锚点 = EL0004**（HCCL 420MB 通信缓冲分配失败，arc_easy 首任务 barrier，core_eval.py:448）——2026-08-28 speedrun Step-7 同款病（当年 401MiB、修复 = core_bs 16→8 已在位），待查本次 argv 与 53d0bcd eval 改动在 ws=8 的交互（0.1738 参考分产自 pre-53d0bcd 代码，锚点本就要复跑重校）；优先级低于双臂。
- **数据处理并行化 + 生成器根因修复（2026-09-16 晚 II，fleet 训练期实施；服务器等臂落地后 pull 两仓）**: prod4 random3b 臂混料 ~1% CPU（192 核）引出的四个热点全修。**根因（nanochat-npu `0942a49`）**: `stream_texts_uniform` 每产出一个 doc 重建 active 读者列表 = O(文件数)/doc → 混料 O(docs×files)，397 个 stem 文件时 28K docs/s 单核就是它 → 增量维护 active（成员+顺序与逐次重建一致 → `random.choice` 消耗同一全局随机序列 → 产出逐 yield 不变；`tests/test_stream_uniform.py` 用逐字旧实现当 oracle：全排干/前缀/损坏-空白-空文件/单文件/全空白/生产式交错（random.random() 与双生成器共享全局状态 + endless 重建周期中途 reseed）全等 + 200 文件×40K docs 11.9×）。**climbmix 侧（本仓）**: ① mix 逐 doc 写盘改 ThreadPool 旁路（`CLIMB_MIX_WRITE_WORKERS` 默认 8，pyarrow 写+压缩释放 GIL；interleave 循环不动 = 确定性红线，workers=1 vs 8 输出字节等价入测试）；tqdm 逐 doc → 逐 shard（省 567 万次 update）。② prepare 落盘抽 `_write_shards` 并行化（索引切片 → 与串行参考字节等价入测试）。③ `upload_dir_if_missing` stat 串行 + 缺失集 ThreadPool 并行上传（`CLIMB_UPLOAD_WORKERS` 默认 8；mount 后端 = 纯文件拷贝线程安全，moxing/esdk 未证实者可钉 1；567-668 片 FUSE 单流 → 并发流）。诚实脚注：本地盘单流 ~160MB/s 饱和，写盘并行收益取决于服务器 /work FS 的并发扩展性；生成器修复与并行上传是无条件收益。测试 test_prod4_fixes 48 项全绿（含新旧组合交叉：mix 字节等价测试现跑修改后的 nanochat 树）。
- **OBS/磁盘完整性包（2026-09-16 晚，prod4 臂舰队等待期实施）**: 消灭"不同配置静默复用旧数据"整类 bug（OBS landmine + 两个本地孪生）+ 磁盘事故防再发。① **OBS mixture 内容键化**: `dispatch_target_arm.py` 的 mixture_uri 从无键 `{arm_root}/mixture_data` 改为 `mixture_data_k{shard_content_key}`（sha1 12-hex over shard 文件名+大小集合）——同配置重试 stat-skip 去重照旧，任何预算/权重/种子/池变化 = 新键 = 全新上传；base_check 空 data_dir 保持 legacy 路径。② **本地孪生 #1**: `prepare_random_baseline.py` 的 `.done` 无条件跳过 → 身份守卫（seed + requested_target_tokens + weights_id；weights 传文件路径时按**内容**哈希），不匹配 fail-loud 提示换 ARM_NAME 或删目录。③ **本地孪生 #2**: `mix_general_data.py` 的 `.done` 只比 stem_ratio → 加 stem_key + general_key 双供给比对（上游 {arm}_shards 换预算重选、或 general 供给变化如 a0df92b 的 +1 安全片都触发重混）；legacy `.done` 一律不可验证 → 重做（fail-safe 方向）。④ **mix 子进程 HF_ENDPOINT**（run4 清单 #2）: `mix_subprocess_env()`，优先级 shell env > launch_env 快照 > hf-mirror 默认；arm_engine.sh 顶部同规则 export（climb/自定义臂的 mix 调用同样受益）。⑤ **磁盘 preflight**（run4 清单 #8 前半）: `scripts/check_disk_budget.py` — 臂新增本地占用估算（prod4 实测 4.5+6.5 GB/B tokens × 1.15 边际，`.done` 已就位产物不计入）vs df 余量，放行条件 free ≥ need×2 + 50G 地板；arm_engine 混料前调用（ARM_DISK_CHECK=0 关闭）。⑥ **上传后本地清理**（run4 清单 #8 后半）: `scripts/clean_derived_data.py` — 默认 dry-run，双守卫（臂未成功 = 重试本钱不删；目录无 .done = 进行中/崩溃残留不删），--force 越过；arm_engine dispatch 成功后自动 --apply（CLEANUP_LOCAL=0 关闭）。共享助手 `shard_content_key` 落 `src/climbmix/utils/io_utils.py`。测试 `scripts/diagnostics/test_prod4_fixes.py` 40 项全绿（键确定性/敏感性、守卫矩阵、估算公式、CLI 端到端）；test_prod2_fixes 回归通过。注意：io_utils + dispatch_target_arm 均 BOTH-stages 未分类 → 双指纹移动；prod4 已过搜索阶段（臂路径不过 stage_gate），**服务器须等臂舰队全落地再 pull**。遗留：OBS 旧无键路径数据（prod4/target_arms/random/mixture_data 1136 片 6B）成孤儿，待一次性 obsutil 清理。
- **prod4 三臂夜跑事故判读 + 修复（2026-09-16 晨）**: 三臂三种死因，逐一闭环。① **搜索完胜**: 111 实测点（54 历史+57 新），生产 w 表与设计逐项吻合（gsm8k w=0.921 主导、其余 5 任务 0.5 地板、[Scoring] WARNING 正常打印）；winner = C10 .474 + C5 .354 + C12 .064 + 12 簇 0.90% 地板（保底精确生效，无零簇，6B 无 shortfall）。② **random 臂远端 7 分钟阵亡 = ws=64 Muon OOM**（`optim.py:516` stacked_grads 随 world_size 线性放大，64 rank 需 4GiB / 29.5GB HBM 仅剩 1.42GiB，所有 rank 在首个 optimizer.step() 同时 exitcode 1）——**8 节点臂对 d28 结构性不可行**，上限 ws=32（4 节点，prod3 3B 双臂实测）；破局需 nanochat 侧分块 reduce（跨仓，战后）。③ **cfg11 臂被 general 供给守卫拦下**（估 44 片=3,724,288 docs < 需 3,726,000+8,075，差 0.046%，fail-loud 按设计工作）→ 修 `a0df92b`：calc_climbmix_count +1 安全片（85K 常数是估计、实测 ~84.6K；cfg11 8.7M stem → 45 片，全套件绿）。④ **winner 臂本地盘满**（Errno 28，混到 95%）：/work 1.6T 100%，6B×3 臂的选样+混料全链本地落地 ~100G + 2 个失败 proxy 残留 16G（成功路径才有清理）。磁盘教训：3×6B 计划没做磁盘预算（20B 方案当时做过 ~720G 检查）。恢复：三臂全走 arm_engine 4 节点并行（避开主链串行 run_arm 债 + preflight 对 FAILED audit 的拦截），主链不再重跑（cp4 手动渲染）。
- **采样器权重地板（簇饥饿保险, 2026-09-15 同日, 继评分修复后）**: 引导探索 `alpha=max(5×w, 0.01)` 使零权重**粘滞**——predictor 宠儿一旦零了某簇，后续轮引导采样几乎不可能复活它（旧 winner 7/15 近零的固化机制；用户点名 Marin 535B 同样讨论过混合搜索的零源问题）。修：`DirichletSampler` 三条采样路径（基础/批量/引导+终选 refinement）统一后置钳位 `max(w, weight_floor)` + 重归一；`SearchConfig.weight_floor` 默认 **0.01**（1%：远低于 uniform 1/K≈6.7%，表达力保留——实测 100% 单簇角点基底探索后仍保持 ~87% 均值，保险有界不废信号）；0 = 关闭恢复旧行为。测试 5 项（批采样地板/重归一、引导路径死簇复活、有界性、关闭语义）；全套件回归绿（prod2_fixes 39 / iteration_floor / reuse_tools / prod2_runtime / 85 launchers / single_pass / read_path 19）。替代原 heavier 方案（alpha 地板/复活变异算子）；明日可选项：no-signal 回退路径的选择时地板（best-measured 仍可能选到稀疏实测点，若明日日志出现 NO-SIGNAL GUARD 再议）。
- **搜索目标静默翻转为 NLL — prod3 winner 反转的根因（2026-09-15, P0, 全部搜索 run 受影响）**: prod3 run4（read_texts 修复后首个有效 run）双臂验证 climb 0.1660 vs random 0.1781（Δ-0.0121，headline 噪音级）——但分任务揭示 gsm8k_cot 0.119→0.052（z≈-6.2）、math_cot_500 0.018→0.004，**生成式数学推理被选择性摧毁**，而 MC 任务持平（arc_challenge 还 +0.0296）且 NLL 微降。执行面排除（2861 步/退火曲线/审计全同；general 后缀分片 20v21 ~95% 同源、有效 token 比 71.6% vs 70.4% 且偏向 climb 多 STEM）。语义地图（balanced_preview + sampled_dataset 探针）：winner 饮食 = C13 百科/词典 .287 + C3 通识散文 .266 + C0 短数学题 .174（~55% 参考书+散文），被清零簇含 C6 抽象代数讲解/C8 数理逻辑论文/C9 小学数学 QA——推理示范被饿死。**根因 = `_compute_scores` SNR 地板两缺陷**：① `σ²_noise=0.25/(K·0.75²)` 把 MC centering 语义套给 CoT 任务（gsm8k centered==raw，chance≈0）+ worst-case p=0.25（实际 p̂≈0.023）→ 噪声高估 ~20×；② `w_floor=0.0` 允许 w 归零 → prod3 终轮 4/6 任务 w=0（gsm8k f_true=+0.85 全场最强信号被判为 f=-1.97 噪声丢弃；math f=-255）→ 目标 ≈ 纯 NLL 排名（score 与 mean(nll_z) r=-0.95，与 mean(acc_z) 仅 +0.85；winner cfg29 与 cfg25 的 stem_metric 0.1468 vs 0.1459 打平、SNR 分差 0.105 全来自 NLL）→ NLL 偏好可预测文本 → 百科/散文 winner。d20 侧佐证：CoT 任务 54 配置全贴地（gsm8k .018-.070 有 spread，"预测期 σ²_between 外推"两个方向都错——MC 高估 40×、gsm8k 低估 5.7×）。**修复**（src 双指纹移位，run4 已收官故无冻结冲突）：① `BENCHMARK_CHANCE`（types.py）按任务声明 chance（MC=0.25，CoT=0.0），未知任务大声回退 MC；② 地板改经验 `p̂(1-p̂)/K/(1-chance)²`（p̂=config 间均值）；③ `w_floor` 默认 0→0.5（NLL 至多分走一半话语权，永不接管——"supplementary"的本意）；④ 每轮打印 `[Scoring] WARNING` acc share + 噪声绑定任务清单（目标构成不再静默）。修正后 prod3 数据回放：gsm8k w 0→0.93、math 0→0.39、arc_c 0.39→0.43。测试 test_prod2_fixes.py 34 项（gsm8k 信号恢复回归：f_old=-2.05 vs f_new=+0.85；w_floor 默认；MC 经验 p̂ 地板）+ iteration_floor/reuse_tools/prod2_runtime 全绿。文档 scoring_metric_design.md §5.1/5.3/5.4/6.2 修订（0.25/K 论据 4 与 w_floor=0 论据被证伪原文存档）。**处置**：54 个实测点 raw 保留 → `rescore_search.py`（已有工具）官方重排 → `run_extend_experiment` 注入历史增量重搜（predictor 在修正分数上重拟合）→ 新 winner 3B 单臂确认 → 20B；旧 winner 20B 轮取消。
- **read_texts 错位根因修复 + prod3 供给彩票修复（2026-09-14, P0, prod1/2/3 全部 STEM mixture 受影响）**: 两独立缺陷把 prod3 iter-1 的 16 个新配置杀到只剩 1 个成功。**缺陷①（供给彩票, d9b6ca9）**: `num_output_files = floor(S/(batch×ratio))` 只留整数余量 r∈[0,7000)，而供给预检要求 5σ 二项余量 ≈3,000 → P(拒绝)≈43%/配置（exp13 仅差 205 docs=0.017% 仍被杀）。修：`calc_output_files()`/`binomial_margin()` 单一事实源（mix_general_data.py），proxy_runner:572/target_runner:298/CLI 三处路由；7 个 prod3 真实数据点回放——4 个被杀全转 PASS（各缩 1 文件）、3 个已通过字节级不变。同批：bootstrapper 迭代 floor fail-loud（n_finite < e_i → RuntimeError，不再静默 WARNING 稀释 predictor）。**缺陷②（read_texts 错位, 本次）**: 铁证链（服务器探针 v1-v4 + 本地确定性重放）：选择侧精确（282,874 docs / 640.3M 配额 / avg 9,055 chars）但 read_texts 交付 avg 2,765 chars、per-doc match 0.03%；重放 mixture 321.1M vs 真实守卫 320.8M（0.1% 误差, 全链忠实, 唯一坏环节=读）。根因 = `column_schema.metadata_read_columns()` 不含 `row_in_shard_col`（读侧 `text_read_columns()` 有——读写不对称），`_read_shard_metadata` 静默 `np.arange` fallback → 元数据缓存全部分片存**行号**而非真 `source_record_idx` → `_check_row_col_sequential` 误判 True → `local_to_row_col` 返回行号 → worker 拿行号当 sri 值做 `WHERE source_record_idx IN (...)`：~82% 命中**错文档**（同分片置换乱序）、~18% 未命中静默 `""`（sri 覆盖 116K/141K≈82% 的数学预期与实测 17.0% 空串率吻合）→ stream_texts_uniform 跳空重抽 → 混料均值上浮签名。**影响：历次 STEM 池 mixture 实为同分片随机文档 → prod1/2/3 搜索信号全部无效；prod3 iter-1 的 9 个"有效点"测的是搅乱数据 → 不注入、从零重开**。修复四件（src/climbmix/data/* → 双指纹移位 → 归档重开）：① `metadata_read_columns` 纳入 `row_in_shard_col`；② `_read_shard_metadata` 配置了列却不可读 → ValueError（杜绝静默 arange）+ sri 唯一性校验；③ `_try_load_cache` 加载时采样 8 分片对账 parquet 真值，不一致 → 大声告警 + 并行重建全部 row_in_shard 列（单列读）+ 原子重写缓存——**自愈服务器现存毒缓存，无需手工删**；④ `_read_one_shard_texts` 四分支缺号/越界 → RuntimeError（拒绝静默 `""`——正是它把硬 bug 变成不可见的 18% 质量漂移）。测试 `test_read_path_integrity.py` 19 项（毒缓存自愈=服务器状态精确复现、old-format 恢复、fail-loud×4、sequential/null 模式回归、spawn 路径）+ `test_iteration_floor.py` 8 项；全套件回归绿。诊断方法论存档：重放探针（object.__new__ stub + exp_0019 真权重跑 _prepare_mixture_data 全链）把"哪个环节坏"从 5 个假设收敛到 1 个，再二分探针定位到读函数内部。

- **跨 run 复用落地 (2026-09-10, docs/reuse_design.md)**: 设计定稿（两层三区存储 / 配置不可变层-派生层模型 / 场景裁决表 / 明确不做清单）+ 四件套实现。① **`scripts/rescore_search.py`**: 读 search_state 的 raw（accumulated_per_benchmark）用当前公式重算派生分数（w/f 表 + 新旧 Δ + top-N + sidecar `<state>.rescored.json`，含公式版本 git SHA），绝不改源文件——评分公式变更（修 bug 或改设计，如 2026-09-10 的 f 单位修复）后的影响评估与注入前预检共用同一条路径；bench 列表默认取 raw 键并集。② **`scripts/inject_history.py` + bootstrapper `history_seed`**: warm-start 种子构建器——多源 state 合并（K 一致 + 4 位小数权重去重 + 丢弃零信息点）、`--pool cluster_cache.npz` 硬校验池 K 并记 sha256、当前公式重算分数后写 `<target>/search_state.json`（历史点 = 超大 iter1：`last_completed_iter=1, realized=[N], pending=None`，旧 run 的 predictor_eval/online_eval/pruning_history/last_c_eff 丢弃，`history_seed` 溯源块：源 run/点数/池哈希/评分 commit/时间）。新 run 零接线：run_climbmix.sh 本就无条件传 `--resume-search`，bootstrapper `_load_state` → `_refit_predictor` 在历史点上重拟合 → iter2 起 guided 采样（`existing_flats` 去重天然避开历史配比）、exp id 从 `len(accumulated)` 续排（无碰撞）、`_reconstruct_iteration_results` 把历史归位 iter1（后续崩溃恢复归因正确）；`history_seed` 在 `_save_state`/`_load_state` 全程往返并大声打印。**CONFIGS_PER_ITER 第 1 槽 = 历史点数**（30 历史 + 60 新 = "30,20,10"）。③ **`prepare_random_baseline.py --weights`**: 均匀 1/K 泛化为任意固定配比（逗号列表 / JSON 数组 / optimal_mixture_weights.json dict 按 C 序号排序；非负和>0 自动归一；维度必须等于池 K；quota 打印与 shortfall 政策同 CLIMB 臂），`.done` 记录 planned_weights——固定比例基线臂与赢家重训的选点步；不带 --weights 行为逐字节不变（旧套件全绿）。④ **`dispatch_target_arm.py --arm <custom>`**: 放开三选一限制，任意 `[A-Za-z0-9_-]+` 名字走通用臂路径（锁/.done/audit/tag/OBS target_arms/<name>/ 均按名泛化，random/base_eval_check 特殊语义只对这两个名字生效）；改训练参数 = CLI 环境变量优先于 launch_env.json（load_launch_env 既有语义，如 `TARGET_STEPS=3000 ... --arm winner_v2`）；臂混合数据 upload_dir_if_missing = 同配比同池复用（§1 per-run 前缀规则是前提）。CP4 用既有 `--arm-a/--arm-b` 成对比较。测试 `scripts/diagnostics/test_reuse_tools.py` 48 项（rescore 单元+CLI+sidecar、注入合并/去重/K 校验/拒覆盖/dry-run、warm-start e2e：guided iter2/realized 记账/id 续排/种子往返/幂等 resume/K 不符丢弃、--weights 单元+真实 parquet 池 e2e（0.6 配比→180/60/60 + shortfall、dict 文件→60/60/150+shortfall_clusters）、dispatch 臂名校验）；回归全绿（prod2_fixes、prod2_runtime、smoke_search、stage_fingerprint、remote_executor 148、search_verbatim 91、random_baseline、embed_dispatch 40、multinode_probes 141、failfast/worker_ledger/two_pass/embed_mock_e2e/eval_pipeline_fixes）。⑤ **阶段脚本 `runs/run_*.sh`**（quadmix 式 `run_<阶段>` = 从该阶段开始；主入口 nanochat 式状态驱动）：`run_search.sh`（主实验三态合一：无 state 无 HISTORY_RUN=从零；同 EXP_NAME 重跑=续跑（三级断点自动续）；HISTORY_RUN=<旧run>=热启动——源 run 校验 → K 一致性预检（balanced_profile K_final vs K_ENHANCED，不一致拒绝）→ 池缓存继承（绝不重新聚类）→ inject → CONFIGS_PER_ITER 槽位核对；state 已存在不重注入直接 resume）、`run_arm_only.sh`（臂阶段：自定义配比 WEIGHTS / 赢家重训+训练参数覆盖 / 已有臂重发（WEIGHTS 空 + --retry-failed）；等池缓存 → --weights 选点 → 混合 → dispatch，.done 幂等）、`run_eval_only.sh`（评测阶段：base 锚点补发；未来 d20 re-eval 挂这）、`run_report_only.sh`（报告阶段：rescore sidecar + 任意两臂 CP4 对比，输入缺哪个跳哪个）。全部：顶部 EDIT 块（`${VAR:-默认}` 风格，env 可临时覆盖）+ `LAUNCH=0` 干跑 + **export 行**（exec 子进程只继承已导出变量——自测抓住的坑）。测试 `scripts/diagnostics/test_run_launchers.sh` 31 项（三态分派：fresh/warm-start 注入/resume 不重注入；K 拒绝、缺源拒绝、槽位警告非 tty 自动续；arm_only 三用法（配比/重发/缺数据警告）；report_only 的 rescore-only/cp4 跳过/空目录拒绝）。**至此所有用户功能 = 一个阶段脚本**。发射谱（复用 prod2 30 点的 prod3）见 docs/reuse_design.md §8。剩余待办（不阻塞）：re-eval 工具（等 mmlu 上游）、preflight 强制 REMOTE_OBS_PREFIX 含 run 名、mark_completed 自动归档 OBS archive/、_save_state 记 pool_cache_sha256。
- **Phase 1.5 LANDED: multi-node EVAL — all nodes join the eval torchrun (2026-09-10)**: Phase 1 conservatively kept eval on node 0 at 8 ranks; a code audit (user-prompted) proved the anchor-comparability concern moot — base_eval shards samples rank-strided (`my_indices = range(rank, len(data), world_size)`, core_eval.py:344/441) and aggregates full-length correct/nlls tensors via all_reduce(SUM), so every sample is scored exactly once and means are world-size invariant (residual = float summation order ~1e-7 << binomial SE ~0.01). Generation tasks (gsm8k/math CoT) use the identical scheme. Changes: ① `remote_worker`: eval_cmd retargets with the same rendezvous at master_port+1 (train's TCPStore may sit in TIME_WAIT); EVERY node runs eval (a node skipping it would hang the rest at init_process_group); per-node private `_eval_base[_node{r}]` (work mount is cross-node shared — make_eval_base_dir rmtree's its target, concurrent same-path builds would race); per-node `eval[_node{r}].log` upload; node 0 still owns CSV/checkpoint/result.json, and its ckpt upload moved AFTER eval (non-master ranks no longer idle at the rendezvous during a GB-scale upload) with a best-effort upload in the failure/exception paths (eval-only retry needs the ckpt); non-master except-path returns 3 WITHOUT writing result.json (master-owned, was a latent clobber). Train-failure path: all nodes skip eval symmetrically (elastic teardown makes rc consistent) → no rendezvous stragglers. Model distribution solved by construction: save_checkpoint writes model from global rank 0 into the shared output mount (probe C: shards from all 4 nodes in one dir) — every node's private eval_base just symlinks it. ② `make_eval_base_dir` + `subdir` param (default unchanged). ③ `dispatch_target_arm.land_logs` also lands `eval_node{r}_{arm}.log`. ④ docs (D15 Phase-1.5 section, parallel_k_selection §5.2). Base anchor (base_eval_check) stays single-node by design. Tests 148 → 151 (eval retarget unit + port+1, per-node subdir, node0 post-eval ckpt, node1 train+eval both rank 1 with port 29511/29512, result.json byte-identity across node1, eval_only refusal unchanged).
- **Phase 1 LANDED: multi-node target arms plumbed end-to-end (2026-09-09)**: d28 arms can now run at `TARGET_ARM_NODES=4` (ws=32, 6.1s/step, ~3.4h/arm vs 10.1h). Six coordinated pieces, tests 100 → 148: ① **spec v3** (`exp_spec.py` +`node_count`/`master_port`; `remote_worker`/`embed_dispatch` lockstep — and fixed the LATENT v2-miss: `embed_worker.py` still gated on spec_version 1, so every embed unit since 9d6c810 would have FATALed "2 != 1"; now 3, test-locked). ② **`nanochat_cmds`**: `build_target_mid_train_cmd` +`load_optimizer` param (None = prod1 argv verbatim; "0" appends `--load-optimizer=0` after `--eval-every=-1`); `retarget_torchrun_multinode` (swaps ONLY the launcher prefix before `-m` — `--nnodes --node_rank --master_addr --master_port --nproc_per_node`, probe-proven order; strict shape asserts) + `resolve_multinode_rendezvous` (platform `MASTER_*`/`MA_*`/`VC_*` triplet → k8s sts-dns hostname/FQDN derivation; anything else raises FAST — a wrong triple hangs every rank at the first HCCL collective). ③ **`remote_worker`**: node_count>1 → every node resolves its own rendezvous + retargets mid_train_cmd; node 0 additionally evals and OWNS result.json; node r>0 exits with its rc after uploading `mid_train_node{r}.log`; per-node streamer names; eval_only+multi-node refuses. ④ **`dispatch_target_arm.py`**: `--node-count` (CLI>env>launch_env, power-of-2 validated — ws=8n must divide d28's power-of-2 dims, run 601cdb67 precedent; base_eval_check refuses multi-node), node_count passthrough into submit (TypeError → clear "needs climbmix-ma adapter" error), `--job-timeout-h` 0=auto (13h/9h), node logs landed, audit carries node_count+load_optimizer. ⑤ **optimizer semantics derived+fingerprinted**: run_climbmix derives `TARGET_LOAD_OPTIMIZER` (nodes>1 → 0 cold, else 1 load), `target_load_optimizer` enters FP_TARGET_PARAMS (TARGET_ARM_NODES itself does NOT — execution shape, num_npu precedent); dispatch cross-checks launch_env value vs its own derivation (disagreement = stale fingerprint, refuse); `target_arm.sh` local fallback emits the same flag from the same var — a remote-cold vs local-warm arm pair would be an unfair verdict. ⑥ docs (parallel_k_selection.md §5.2 multi-node entry; paper_deviations.md D15). Worker-level integration covered by a REAL-subprocess simulation (stub torchrun records argv): single-node argv untouched, node0 train retargeted + eval standalone, node1 train-only rank 1 + own log + result.json untouched. Remaining before first 4-node arm run: server-side nanochat tarball must be rebuilt from `2b52534` (loader fix) + resources re-uploaded; observed first-step losses vs historical shape as the live parity check.
- **Phase-1 blocker CLEARED: loader global round-robin sharding, provably non-disruptive to single-node (2026-09-09)**: nanochat-npu `2b52534` replaces `_document_batches`' per-file DDP row-group sharding (rg_idx = global rank, stride world_size WITHIN each file — starves ranks >= a file's rg count, the Phase-0 root cause) with a global (file, rg) round-robin: all row groups flattened in (file-order, rg-ascending) sequence, global index k -> rank k % world_size. **Equivalence proof**: when every file's rg count is divisible by world_size (every production shape: 16-rg mixture shards at ws in {1,2,4,8,16}), the file offset f*R vanishes mod ws, so the assignment degenerates to exactly the old per-file scheme — same ranks, same row groups, same order, same (pq_idx, rg_idx, epoch) yields, same resume-state semantics — existing single-node runs and mid-run checkpoints are unaffected. Verified by 44 checks (`/tmp/opencode/test_loader_sharding.py`, real dataloader.py under stubbed torch/common): fresh-start sequence equivalence (ws 1/2/4/8/16 x 3 file shapes x train/val, full doc lists compared), exhaustive resume equivalence at EVERY save point (old-scheme reference vs new, mid-rg and epoch-final states), coverage + zero overlap + balance at ws=24/32, starvation RuntimeError when TOTAL rgs < ws (per-file min would wrongly reject valid setups like 4 files x 4 rgs at ws=8), corrupt-shard uniform drop at a footer-only pre-scan (old scheme made corruption rank-visible), mid-run file loss skip. Production mixture (16 rg/file) now works at ws=32 with no data repartition; probe C's staging repartition (f580cc5) becomes optional belt-and-suspenders. Remaining Phase 1: nanochat_cmds multi-node torchrun builders + --load-optimizer=0, dispatch_target_arm node_count=4/timeouts, remote_worker node-role branches + local materialization, preflight, docs (node_count restricted to powers of 2 by optim.py:499).
- **Phase 0 CLOSED: probe C GO at ws=32 (4 nodes) — 6.1s/step, 3.0x vs ws=8 (2026-09-09)**: job 440f760e (4×910B, dns-sts rendezvous): 50 steps mean dt 6.1s vs the 7s ws=32 gate (18.2s ws=8 anchor → 3.0x; a 2000-step target arm drops 10.1h → ~3.4h), 34 optim ckpt shards across all 4 nodes (10/8/8/8 — cross-node checkpointing verified), rc=0 everywhere; the first-collective gate saw all 32 ranks arrive in ~1s. The full failure chain that preceded GO, every link now explained: (1) a08efac unquoted-glob call-site bug (fixed 16b3aa9); (2) row-group starvation — mixture shards carry 16 row groups per file and `_document_batches` shards row groups per-file rank-strided, so at ws>=17 top ranks get zero row groups and spin reopening ParquetFile forever (fixed for the probe by f580cc5's staging repartition to 48+ rg; nanochat 44ba8ff raises loudly at startup instead of spinning); (3) ws=24 (3 nodes) is STRUCTURALLY invalid for this optimizer — optim.py:499 asserts `shape[0] % world_size == 0` for large AdamW params and all d28 tensor dims are powers of 2, so world_size must be a POWER OF 2 (2 or 4 nodes; never 3; ws=32 also divides total_batch_size 1048576 exactly — no dt bump). The platform was never sick: the "39% bad node" statistic, the obsfs-hang theory, and the EVS cold-read theory were all misreadings of (2)+(3); the platform ticket is CANCELLED. Residual Phase-1 constraint: production arms train on the REAL mixture shards (16 rg/file) — the 44ba8ff assertion will (correctly) refuse to start at ws=32 until either the loader sharding is fixed (global round-robin over (file, rg) pairs; resume-state semantics need care) or the mixture is repartitioned once. Tests 141.
- **probe C ROOT CAUSE FOUND (misdiagnosis retracted): row-group starvation, not sick nodes (2026-09-09)**: run 20260909_165952 (job e3fae7f7, 3 nodes) — the fast-fail kit finally caught it red-handed. Pre-flight PASSED on all 3 nodes (4 shards, every row group, 0.3-0.4s — storage healthy), then node-2's ranks 16-23 spun at ~3400 log lines/s for 2.5min: endless `ParquetFile.__init__ enter/ok in 0.0s` cycling shard_00000/00001/00002 (~540k lines, 64MB/rank log) with NO read_row_group progress and NO completed loader next(), until rank 19 **SIGSEGV'd** (exitcode -11, torchrun tore the job down at 8min; ranks 0-15 were correctly parked at the first-collective gate). Mechanism: `_document_batches` DDP-shards row groups PER FILE — `rg_idx = global_rank`, stride world_size (dataloader.py:71) — and the real mixture shards have only **16 row groups per file**, so at ws=24 ranks 16-23 get ZERO row groups from every file: the `while True` generator loops forever reopening files (dataloader.py:56 — exactly the line the run-9 heartbeats sat on) without ever yielding → never reach the first collective → the other 16 ranks wait → EI0015 at ~20min. **The "39% sick nodes" statistic and the run-9 "60s/op cold-read crawl" were BOTH misreads of this same spin** (run 9 predated the per-op duration patch; frame movement between heartbeats is indistinguishable from fast re-opens; the "sick host" each run was just whichever machine drew node 2 = ranks 16-23). Runs 3/4/6's "obsfs hangs" were most likely the same starvation with obsfs-slow ParquetFile opens (pre-materialization) — the platform ticket is ON HOLD until a probe goes green. Fixes: (1) `repartition_shards` in run_multinode_probes.py — probe C staging now rewrites each shard to >= `--probe-row-groups` (default 48; 8/16/24/32-rank-safe) row groups before upload, `min_required=ws` fails loudly at staging if unreachable; nanochat stays untouched for the probe. (2) nanochat-npu dataloader.py: starvation guard — raises `RuntimeError` at generator start if `world_size > min row groups per file` (production protection). **PRODUCTION BLOCKER NOTED**: mixture shards carry 16 row groups < 24 — Phase-1 multi-node target arms (ws=24/32) would starve identically; needs either the loader-sharding fix (global round-robin over (file,rg) pairs — bigger change, resume semantics) or a data repartition before Phase 1. Tests 136 → 141.
- **probe C: parquet cold-read pathology pinned; pre-flight + pyarrow breadcrumbs (2026-09-09)**: run 20260909_154328 (job a3cd7236, 3 nodes) — the heartbeat stack dumps NAILED the hang: node-2's 8 ranks spent 23min inside `pyarrow/parquet/core.py ParquetFile.__init__` (footer read/schema parse) called from `dataloader.py:56 pq.ParquetFile(filepath)` in `_document_batches`, i.e. the generator body's first `next()` at mid_train.py:247. Frames MOVED between dumps (core.py 331→344→369→327, ~60s per read op) = degraded per-node storage latency, not a frozen syscall; marker thread alive (GIL fine); files are the LOCAL materialized copies (`list_parquet_files` uses `--data-dir` directly — verified) and the same disk served the 7.4GB model in 9-14s from page cache — a cold-read pathology (likely network-backed /home/ma-user/work on that node). Healthy nodes parse the same shards in seconds. Not obsfs, not code. Hardening: (1) `probe_parquet_check` in probe_common.sh — pre-torchrun per-node gate that opens every shard and reads EVERY row group under an outer `timeout` (PROBE_PARQ_TOTAL default 240s, per-file soft budget PROBE_PARQ_PER_FILE 60s) → visible train_failure marker instead of a 20min EI0015 burn, and it warms the page cache for every row group the distributed loaders touch (if the pathology is cold reads, the pre-flight may even cure it); called from probe_train.sh after the dep loop. (2) wrap now patches `pyarrow.parquet.ParquetFile.__init__` (every call: path + duration + row-group count) and `.read_row_group` (first 3 + 50ths, plus a "(SLOW)" line when any call exceeds 10s) — next run times each op per rank. Stub e2e now reads a REAL parquet through the patched path. Tests 130 → 136. **[SUPERSEDED 2026-09-09: the "60s/op crawl" was the row-group starvation spin — see the row-group starvation entry above; the pre-flight + pyarrow patches remain as instrumentation]**
- **probe C first-collective gate: 25min → ~10min fail-fast (2026-09-09)**: the recurring ~25min failure wall is ~4min boot/materialize + **~20min HCCL communicator-creation internal timeout** (EI0015; `HCCL_CONNECT_TIMEOUT=600` provably does not shorten it on this CANN — runs 20260908_201827/20260909_102807/1129xx/145450/a3cd7236 all EI0015'd at ~20min) + teardown. The wrap now arms a gate at the FIRST data collective (any of all_reduce/reduce_scatter/all_gather/broadcast/barrier, i.e. exactly where EI0015 would hang): each rank sets `probe/arr/{rank}` in the c10d default store, polls every 2s, and if not all `world_size` keys appear within `PROBE_FIRSTCOLL_TIMEOUT_S` (default 300) it bc-logs + raises `RuntimeError` naming the missing ranks AND their node indices (`r // LOCAL_WORLD_SIZE`) — healthy ranks abort in ~5min instead of 20, and the failure cleanly separates "hung BEFORE any HCCL call" (gate fires; hung ranks' heartbeat stacks have the frame) from "hung inside HCCL" (gate passes, EI0015 as before). Gate degrades to ungated passthrough if the store is unreachable. Stub e2e now ships a torch.distributed PACKAGE (with distributed_c10d fake store) and proves both paths: ws=1 → "all 1 ranks arrived" → collective proceeds; PROBE_TEST_WS=2 (rank 1 never appears) → nonzero exit in <30s with "gate FAILED: 1/2 ranks never reached … missing ranks [1], node(s) [0]". Tests 126 → 130.
- **probe C heartbeat stack dumps + generator tracing (2026-09-09)**: run 20260909_145450 (job 7b2165de, 3 nodes, first run with materialization + quoted globs) moved the hang — all 3 nodes materialized inputs cleanly (model 10-14s, tokenizer/data instant) and node-2's ranks cleared `load_model` (9-10s, LOCAL disk) → `get_token_bytes` → loader FACTORY call, then went silent ~20min; nodes 0/1's 16 ranks died at the first HCCL communicator creation (EI0015 `rank num[24] vs rank list size[16]` = exactly the healthy set, ~20min after the last node-2 breadcrumb — HCCL_CONNECT_TIMEOUT=600 still not shortening it). Root insight: the two flat loaders are GENERATOR functions — the wrap's `done in 0s` only proves the generator OBJECT was created; the body (parquet reads via `_document_batches`, rustbpe encode of ~1000 buffered docs, numpy assembly, H2D `gpu_buffer.copy_`) executes on the FIRST `next()` at mid_train.py:247 — exactly the uninstrumented gap. obsfs is now exonerated for this stage (everything reads local disk). Wrap upgrades: (1) `faulthandler.dump_traceback_later(period, repeat=True)` — a C-thread watchdog that dumps all-thread stacks every `PROBE_HEARTBEAT_S` (default 60s) into wrap_rank{N}.log even when the main thread starves the GIL inside a native call, plus a timestamped Python marker thread (markers stop but dumps continue → GIL-holding native hang; both stop → process gone); (2) `_trace_generator`/`_patch_gen_factory` — the two flat loader factories and `_document_batches` now trace every `next()` (first 3 + every 50th, enter/ok/RAISED with duration). Local stub e2e proves the heartbeat names the exact stuck frame (`mid_train.py, line 14`) while the traced generators mark next boundaries. Tests 124 → 126.

- **probe C arg-shift bug + decision-table evidence (2026-09-09)**: run 20260909_141758 (job de55adab, 3 nodes) failed uniformly in ~3min with `mkdir: cannot create directory '.../tokenizer.pkl': File exists` — root cause was MY a08efac call-site bug, not obsfs: `probe_mat_cp "$IN"/tokenizer_0/* dst label` left the glob UNQUOTED, so bash expanded it at the call site into 2 words and shifted dst/label by one (cp then targeted tokenizer.pkl the FILE; d28 model/meta survived only because their globs matched exactly one file). Platform news buried in the noise: all 3 nodes materialized the 7.4GB d28 model in 14s — obsfs read path healthy this run. Fixes: (1) all 4 probe_train.sh call sites now pass the pattern as ONE quoted arg (`probe_mat_cp "$IN/tokenizer_0/*" ...`) — probe_mat_cp expands it internally, as its `src_glob` param always intended; (2) print_decision_table gained c_dir and surfaces `train_failure_*.log` rows when no train_result landed (this run printed an empty table + "see rows above"); B-fail early-exit call updated too. Tests 120 → 124: call-site lint (every probe_mat_cp line in probe_train.sh must be fully-quoted-with-glob), multi-file quoted-glob function test (2 files → both copied to dst dir), decision-table failure-row render, train_verdict(None) still FAIL. Local mat_cp tests had exercised the function but never the multi-file CALL SITES — the old test even pinned the buggy unquoted form (assert updated).
- **train gate interpolation for arbitrary world size (2026-09-09)**: `train_verdict` previously bucketed ws=24 under the ws=32 7s gate. Now linear between calibrated anchors: ws=16 → 11s, ws=32 → 7s (ws=8's 18.2s anchor scales to ~6.1s at ws=24; mid_train auto-bumps total_batch_size 1048576 → 1081344 at ws=24, a conservative +3% on dt). 3-node jobs: gateway accepted node_count 2 and 4; 3 rides the same StatefulSet mechanism (accepted at submit time in run 20260909_141758 — job de55adab went RUNNING, so the mechanism holds). Tests 119 → 120.
- **多节点目标臂 Phase 0 探针落地（2026-09-08，4 节点设计 / 2 节点先行）**: prod2 后目标臂瓶颈 = 单节点 ws=8 训练 2000 步 ≈ 10.1h/臂（18.2s/步实测锚点）。可行性结论：nanochat-npu **零改动** — 梯度同步本就是"每 optimizer step 一次 reduce_scatter+all_gather"（无 DDP wrap，optim.py），`total_batch_size` 固定 → `grad_accum_steps` 派生（ws 翻倍 = 每 rank 计算减半，优化语义不变），HCCL backend + env:// init，model 单文件 ckpt（任意 ws 可加载）。唯一语义断点 = d28 optimizer 分片（8 片，ws≠8 加载必坏）→ 决策 **`--load-optimizer=0`**（冷启动有 bias correction（optim.py:97）+ Muon 零动量 = 从零训练同路径，有界；两臂同形态 → climb-vs-random 判决不受影响；CLIMB 论文对外部 base 本就拿不到 optimizer state，不加载反而贴论文语义）；残余：臂自身 ckpt 的 32 片 optim 分片若未来跨 ws 续训才需 reshard 工具（通用 N→M，缓建）。平台侧未知数（网关收不收 node_count>1 / 每节点执行同一 command / 注入什么身份 env / 跨节点网络）由三连探针回答。落地：①climbmix-ma `submit()/submit_raw()/​_build_job_body` 增加 `node_count` 参数（默认 1，向后兼容；测试 183→193）。②`scripts/probe/`：A 身份探针（env dump + 密钥脱敏 + 输出挂载跨节点可见性 marker 测试）、B HCCL 探针（1GiB allreduce 带宽 + 小包延迟 + barrier，rank0 写 JSON）、C 训练基准（50 步 ws=16 dbs=1 `--load-optimizer=0`，d28/tokenizer/data 走 input mounts，nanochat tar 走 code dir；抓 `dt:` 行均值 + Peak memory + 跨节点 optim 分片落盘验证）、`run_multinode_probes.py` 驱动（A→分析→选 rendezvous 策略→B→C→决策表：门槛 ws=16 ≤11s/步、ws=32 ≤7s/步）。③rendezvous 三路径：平台 env（`*_MASTER_ADDR` 三元组，优先级 "" > MA_ > VC_）→ 输出挂载选举（marker 数值 IP 排序选 master，`awk FNR==1` 抗缺尾换行）→ static。④指纹：`scripts/probe/` 加 `GLOBAL_EXCLUDE_PREFIXES`（同 diagnostics 先例）+ **fingerprint.py 自排除**（分类器自哈希 = 每次分类编辑都白移指纹；一次性移位 9b2c→f37a on pull，之后分类编辑不再移位）。探针全走独立 OBS prefix + 独立 clone，不碰生产 run 目录。测试 `/tmp/opencode/test_multinode_probes.py` 58 项（bash -n、rdzv 选举数值序/超时/部分三元组、决策门槛、probe A 本地 e2e、tar 布局/守卫）。探针用法见 `run_multinode_probes.py` 头注；执行注意：池子 80 卡被搜索占用时 2 节点=16 卡可插，4 节点等空窗。**Probe A 第一轮实测（2026-09-08，node_count=2）**：网关接受、两节点各执行 command（172.16.2.196/172.16.4.245 跨网段）、11 个平台身份 env 候选全空、输出挂载 90s 内跨节点不可见 → 平台 = K8s StatefulSet（hostname=`ma-job-<id>-worker-N`，/etc/hosts 带 pod FQDN `<host>.<svc>.<ns>.svc.cluster.local`）→ **新增 dns-sts rendezvous**（master=worker-0 的 FQDN 经 files-then-DNS 解析为 IP、rank=hostname `-worker-N` 后缀；优先级 platform > dns-sts > output-sync > static；probe A 新增 `sts_dns` 探测汇总，驱动自动选路；probe_common.sh 的 `rdzv_dns_lookup` 模仿 nsswitch files-then-DNS，`PROBE_HOSTS_FILE` 可注入）；探针产物改按 `probe_{tag}/{ts}/result` 按运行隔离（同 prefix 重跑不串档，旧版会混入上一轮 node_summary）。测试 58→73 项（sts 推导/自选主/非 worker 主机名拒绝/主不可解析净败/auto 链、analyze 聚合、e2e per-run 路径）。**Probe B 实测（2 节点 ws=16）**：busbw 111.79 GiB/s（RoCE 顶级）、小包 0.57ms/barrier 0.67ms → 通信税可忽略，预测 ws=16 ≈9.3-9.6s/步、ws=32 ≈4.7-5s/步。**Probe C 首跑（4 节点 ws=32）失败**：worker 路径的 boot 会离线装镜像缺的依赖（rustbpe/pyarrow 等非 vllm 依赖，wheel 由 ma_sync_code --wheel 传到 {prefix}/assets），探针的 shell 命令路径没有这个 boot → `ModuleNotFoundError: rustbpe`（nanochat/tokenizer.py:160 模块级 import，非 pip 可装的 Rust 扩展）。修复：驱动 `--wheel-from`（缺省只读共享 {obs_prefix}/assets 的 wheel 到探针 code dir，同 d28/tokenizer 共享类）+ probe_train.sh 复刻 boot 的 dep 循环（import 探测→离线 wheel→pip→华为内网镜像，同款依赖表）。**重跑再遇两坑**：①wheel 不在 {prefix}/assets（那是 fresh channel，只有 worker 代码）→ 默认扫描源改为 assets/ + assets_big/（一次性 bundle，d28 同级）；②网关 ModelArts.2791 "tokenizer/ is not a directory" 拒绝了 20 分钟前刚接受过的同一输入表（搜索集群同 bucket 高压下网关 OBS 目录校验抖动；适配器本就把 2791 归为 TransientSubmitError）→ 驱动三个探针提交统一加重试网（TransientSubmitError 退避重试 4 次），并在上传 tar 之前本地 list 验证 d28/tokenizer 输入（空目录=秒级 SystemExit，不再浪费 30 分钟吃网关 400）。测试 82→88 项（重试成功/耗尽、双 wheel 源、verify_input 三态）。测试 73→82 项。**Probe C run 3（4 节点 ws=32，依赖引导成功）新故障定位（2026-09-08）**：rustbpe 装上、dns-sts 会合成功、4×8=32 rank 全部拉起，但 worker-3（172.16.7.200，唯一未验证网段）的 8 个进程在第一行日志之前**静默挂死**（mid_train 首打印在 compute_init 之后——common.py:208 set_device → :216 init_process_group，两个挂点日志上无法区分），其余 3 节点跑完模型加载+step-0 forward/backward 后在首个 32-rank 集合通信（mid_train.py:407 NaN 检查 all_reduce）等它到 HCCL EI0015 超时（rank num[32] != rank list size[22]）；该节点 4 网段并存（w0=2.x master/w1=3.x 通/w2=4.x 通/w3=7.x 挂——B 探针此前只验证过 2.x↔4.x）。修复（挂点归因三件套）：①probe_common.sh `rdzv_tcp_probe`——rendezvous 后、torchrun 前对 master:port 做 TCP 探测（refused=路径通未监听（正常）/timeout=丢包（w3 故障形态）/open/skip 四态 + rc/ms），结果进 rdzv_resolved json 的 `tcp/tcp_rc/tcp_ms` 字段，`PROBE_SKIP_TCP_PROBE`/`PROBE_TCP_TIMEOUT`（缺省 5s）可调，非致命仅留证；②probe_hccl.py `bc()` 面包屑——每 rank 每阶段（start/torch_npu imported/set_device/init_process_group(hccl)/init ok/allreduce done）print+追加写 `$PROBE_OUT/bc_rank{N}.log`（挂住的 rank 文件停在挂点，异常路径原本就写 error json，挂死路径靠面包屑）；③probe_hccl.sh torchrun 输出重定向到 `$OUT/hccl_torchrun_$(hostname).log`（EI0015 证据进下载）+ B/C 双探针 `HCCL_CONNECT_TIMEOUT=600`（挂死 10 分钟出 EI0015，不再冻结 20 分钟）；probe_train.sh 增加 `boot_$(hostname).log`（启动阶段 echo tee 进输出挂载——控制台日志不回流）。测试 88→96 项（tcp 四态、rdzv_resolved json 字段、bc 阶段顺序、run_bash 助手前置）。**Probe C run 4（换 exp_0000/mixture_data 数据，10:28）再失败 + 设备面根因锁定（2026-09-09）**：tcp 探测 4 节点全部 refused（10-12ms）→ pod 网络全通（含 6.x/7.x），“网段被墙”假设否掉；w0/w1 跑完 step-0 计算后在首个集合通信 EI0015（rank num[32]≠rank list size[22]，~20 分钟，HCCL_CONNECT_TIMEOUT=600 未生效——非该 CANN 版本的正确旋钮或超时在别处）；w2(172.16.7.226)/w3(172.16.6.123) 静默挂死（0 content lines）。**跨两轮统计：挂死节点全部来自 6.x/7.x 网段（run 3: 7.200；run 4: 7.226+6.123），2.x/3.x/4.x 全部健康**；B@4 通过是因为恰好落在健康节点。结论：挂点在 HCCL 设备面（RoCE fabric/设备 IP，与 pod 网络两张网）——部分节点的设备面组建不了 32-rank 域（经典设备 IP 冲突或 fabric 隔离，平台侧问题，非代码问题）。C 探针盲区修复（`probe_train_wrap.py`，torchrun 入口→面包屑→runpy mid_train 零改动）：设备打开+微小计算（wedged NPU）/ MASTER_ADDR:PORT store TCP 连接（此时代理 store 应在监听，与 rendezvous 前的 tcp 探测互补）/ `init_process_group` 与全部 collective（前 3 次+每 50 次）patch 进出入标记——非 master 节点从 import 到首个集合通信之间的任意挂点不再隐形；面包屑 print+追加写 `$PROBE_OUT/wrap_rank{N}.log`。配套：`rdzv_dump_host_nets`（probe_common.sh，/etc/hccn.conf + npu-smi + ip addr 尽力取证，B 写 host_nets_*.log、C 进 boot 日志，为平台工单取证）、probe_train.sh torchrun 入口换 wrapper。测试 96→110 项（stub torch/torch_npu/scripts.mid_train 全链路功能测试：面包屑顺序、argv 透传、节流、store 连接 ok/FAILED 两路、host-net dump 容错）。**Run 5（11:12）rc=2 自伤修复**：wrapper 改造把 torchrun 入口的 `--` 分隔符弄丢 → mid_train 的 `--run=probe_c` 被 torchrun 自己的 argparse 吃掉（`--run-path`/`--run_path` 二义，"ambiguous option" 秒退 rc=2，4 节点统一）；加回 `--`（argparse REMAINDER 复现实证：带 `--` 时 training_script_args 干净直达 wrapper，`--` 本身被吃掉，wrapper 无需剥）。回归测试补 `--` 分隔符源检查 + 复现 parser 检查（本地 stub 测试直接 python 调 wrapper、不经 torchrun argparse——盲点成因，源检查兜住）。测试 110→112 项。**Run 6（wrapper 版，11:28）真相大白——设备面理论推翻，真凶 = obsfs 输入挂载读挂死（2026-09-09）**：wrap 面包屑证明 32 个 rank 全部通过 device compute ok / store tcp connect ok / init_process_group ok（后者需全 32 rank 到 TCP store 报到）——NPU、pod 网络、torch PG 全部健康；节点 2/3 的 16 个 rank 之后在 mid_train 非通信引导段（load_model 读 obsfs 上的 7.4GB d28 / tokenizer / parquet loader）静默挂死，节点 0/1 在首个集合通信等它们至 EI0015（本轮 rank list size[16] = 恰好健康节点的 16 rank，数字对上）。统一解释全部历史：B 探针 3 战 3 胜（**不挂输入盘**）、C 探针 3 连挂（每次 1-2 个节点 obsfs 读挂死）、"6/7.x 网段"实为那些节点的 OBS 存储网退化（与 HCCL 无关）。对 Phase 1 的预警：生产臂作业的 d28 同样走 obsfs asset mount，同坑。修复两件：①`probe_mat_cp`（probe_common.sh）——输入物化到本地盘（per-file 超时 900s 可调，挂死/不可读 → train_failure 标记 + 该节点 5 分钟内可见地失败，不再冻 20 分钟），probe_train.sh 改物化（load_optimizer=0 → checkpoint_manager 只在 load_optimizer=True 才读 optim_*.pt（:101-108），故只拷 model+meta ~7.5GB，跳过 10GB optim 分片），训练路径 obsfs 清零；②wrapper 补 nanochat boot 阶段补丁（load_model/get_token_bytes/flat loader 的 from-import 在 runpy 时绑定 → 提前 patch 源模块即可拦截所有调用点，nanochat/__init__ 为空无副作用）——init_process_group 到首个集合通信的盲区关闭。测试 112→119 项（mat_cp 三路：成功/零匹配/不可读；nanochat 补丁 from-import 拦截行为测试；物化源检查）。

- **prod2 B++ 全量版落地(2026-09-07,~40h→~24h)**:①`runs/lib/target_arm.sh` 共享库 + `run_climbmix.sh` Step 6+7 重构为 `run_arm()` 三层(标记→远端 dispatch→本地兜底)+ S1 交错评测(训完即评,eval 提前 ~10h 可见);argv 与 python 构建器逐 token 对齐(`nanochat_cmds.build_target_*`,测试强制)。②本地 k=8 整槽(`REMOTE_LOCAL_PARALLEL=1` 显式开启,`npu_per_exp == npu_devices` → 1 个串行全卡槽,`_local_slots` `<`→`<=`)。③自适应三机制(`ADAPTIVE_CONFIGS=1` 显式开启,默认字面):期望列表 `CONFIGS_PER_ITER="20,10,10"` → 波预算 `w_i=round(e_i/S0)`;`AdmissionController`(remote_executor)探窗测 `C_eff` 截未提交尾部(guided 轮按预测器排名头排保头)、滞留驱逐(PENDING>30min 且同批在跑 → cancel+永久移出,pending 原子重写)、滚动定尺寸(iter≥2=`w_i×C_eff_prev`);护栏 C_eff<2/池<4 禁截断。④`scripts/dispatch_target_arm.py`:random/climb 双臂远端 8 卡作业(与搜索舰队同池同镜像同 argv)+ `--arm base_eval_check` 远端 d28 base 锚点(`--model-type=base`);d28 资产一次性引导上传;训成评败抢救 checkpoint 落 `.done_mid_train`;per-arm flock 双 dispatch 互斥;`launch_env.json` 快照使独立 nohup 不依赖发射 shell 环境。⑤`scripts/diagnostics/prod2_watch.sh` CP0-CP4 仪表盘。默认值翻新:`MERGE_STRATEGY=balanced`、`TARGET_ARM_MODE=remote`、`K_CLUSTER_MAX=K_ENHANCED`、`REMOTE_LOCAL_PARALLEL=0`/`ADAPTIVE_CONFIGS=0`(保守默认+显式开启)。ExpSpec v2(+`ckpt_src`,worker fail-fast:base 资产缺失清晰报错)。测试:`scripts/diagnostics/test_prod2_runtime.py` 45 项 + `/tmp/opencode/test_remote_executor.py` 扩到 147 项(新增 admission e2e:mock 舰队 8 配置探窗截到 4、stats/rolling C_eff/dropped 断言);全部回归绿(prod2_fixes 22、search_verbatim、smoke_search 18、json_finite、embed_dispatch 40、embed_mock_e2e 40、worker_ledger、p0、two_pass、stage_fingerprint、fixes)。决策记录 `docs/parallel_k_selection.md` §5.2;发射线:`EXP_NAME=prod2_k15bal K_ENHANCED=15 NPU_PER_EXP=8 REMOTE_MAX_JOBS=10 CONFIGS_PER_ITER="20,10,10" ADAPTIVE_CONFIGS=1 REMOTE_LOCAL_PARALLEL=1 ... nohup bash runs/run_climbmix.sh &` + `nohup python3 scripts/dispatch_target_arm.py --arm random &`

- **Result-dir lifecycle (`ls result/` self-describing)**: replaced the ambiguous `${EXP}_stale_<ts>` / `${EXP}_target_stale_<ts>` zoo with a two-class layout — ACTIVE `result/${EXP_NAME}_current/` vs HISTORICAL: completed runs `result/${EXP_NAME}_<ts>/` (terminal `.done` markers present; renamed automatically at the end of a green run by `mark_completed` in `runs/lib/stage_gate.sh`) and abandoned runs `result/${EXP_NAME}_stale_<scope>_<ts>/` (scope = search/target/legacy/orphan, what changed). Every archive carries `archive_meta.json` (reason, archived_at, old→new fingerprints, git HEAD, was_complete, moved items) — the dir NAME no longer has to carry the whole story (fixes the "weird mtimes / which dir is what" confusion of 2026-08-27/28). Re-running a COMPLETED experiment is idempotent: if `_current` is missing, the newest `${EXP_NAME}_<ts>` whose SEARCH fingerprint matches is restored as `_current` (all `.done` skip, zero NPU; a target mismatch then re-runs only Steps 4-8 — the eval-every scenario). Old server layouts (`result/$EXP_NAME`, `*_stale_<ts>`, `*_target_stale_<ts>`) auto-migrate to the new names on the next run (idempotent one-time). COMPLETION_MARKERS per shell: speedrun `.done_eval_climb`; run_climbmix both arms. Deferred (discussed, not built): fingerprint params sidecar (`.fingerprint_search.params`) to diff old-vs-new params at archive time — only matters for env-var overrides that bypass git; ~15 lines in stage_gate. Guarded by `/tmp/opencode/test_stage_fingerprint.py` (48 → 77 checks: L1 old-naming migration + idempotence, L2 completion rename incl. missing-markers case, L3 restore + re-complete + restored_from trail, L4 mismatch no-restore, L5 newest-match wins; meta field assertions on search/target/legacy archives)

- **d28 Step-7 eval OOM fix (core-eval-batch-size 16→8, device-batch-size 32→16)**: 2026-08-28 speedrun reached Step 7 and the FIRST task (arc_easy) died at its final `dist.barrier()` (core_eval.py:412): HCCL could not allocate its 401MiB allreduce comm buffer (`Memory_Allocation_Failure EL0004`) — an acl-level allocation OUTSIDE torch_npu's allocator accounting. Mechanism: `--eval=core` never reads `--device-batch-size` (BPB-only, base_eval.py:514/:522 — so quadmix's dbs=16 "working" was never evidence about core eval); the REAL core-eval memory knob is `--core-eval-batch-size` (default 16, base_eval.py:417) which pads the whole chunk to the longest sequence in ONE forward (logits B×T×V dominates). max_per_task=100 → 13 examples/rank → one 13-wide padded block pushed the torch pool to its ~24.5G ceiling, and the per-task `empty_cache()` (base_eval.py:366) only runs AFTER the barrier → the first collective had <401MiB free. 8x910B3 (64G HBM) ran the same default fine; 8x910B4 (32G) halves headroom → halve the batch. Fix (climbmix side only, zero nanochat-npu edits): `EVAL_DEVICE_BATCH_SIZE` default 32→16 (BPB-only knob, quadmix-aligned, future-proofs bpb) + NEW `EVAL_CORE_BATCH_SIZE=8` wired to `--core-eval-batch-size` in speedrun Step 7, run_climbmix `run_eval` (both arms), and `target_runner._build_eval_cmd` (hardcoded 16/8, same precedent as its mid_train dbs=1); `eval_core_batch_size` added to `FP_TARGET_PARAMS` in both shells. proxy_runner eval deliberately untouched (d20 + 1 NPU/exp, all 21 speedrun exps passed with defaults). CONSEQUENCE: target fingerprint changes → stage gate archives target products → Steps 4-8 rerun incl. the just-finished Step 6 (search products kept). Known residual (deferred, needs nanochat-npu edit): the MC-path OOM retry (core_eval.py:382) only matches 'out of memory' — NPU's EL0004 string doesn't match, so the halving fallback never fires on 910B4; core_bs=8 leaves ~2G+ slack making the patch non-essential. Guarded by `/tmp/opencode/test_eval_oom_corebs.py` (defaults, cmd flags, FP membership + search-stage untouched, target_runner alignment, mid_train config intact, bash -n, live fingerprint-module semantics)
- **stem_ratio injection fix**: proxy_runner/target_runner load mix_general_data via importlib and called `mix_data()` WITHOUT passing their ratio — the mixing coin flip used the hardcoded module global `STEM_RATIO=0.7` while the shard-count calc (`calc_climbmix_count`) used the runner's `self.stem_ratio` → silent divergence for any `--stem-ratio ≠ 0.7` (also a parallel-thread shared-global hazard). `mix_data(stem_ratio=...)` now honored end-to-end (both runners pass it), range-validated (0,1), `.done` records the ratio, and a stale `.done` from a different ratio triggers a remix (fingerprint archives normally cover this; the guard covers direct CLI reuse of an output dir). `/tmp/opencode/test_p1_stem_ratio_report.py` verifies exact coin-flip counts (same seed → same uniform sequence compared against each threshold), `.done` audit fields, and the runner call sites
- **Target-stage CSV attribution verified + per-arm archival**: 03c0527's private-base-dir evals fully resolve the PROXY path (CSV collision physically impossible). target_runner.py path (currently unused by the shells — run_climb.py runs `--skip-target`): sequential evals + 3-tier `_locate_eval_csv` (tag-in-name → mtime window → newest fallback) + archive copy to `run_dir/eval_{tag}.csv` — attribution reliable. Shell path (production): Step 7 evals sequential and Step 8 parses LOGS not CSVs; only gap = both arms' raw CSVs share the step-only name `mid_model_{step}.csv` in the shared base dir (second eval overwrites the first) — `run_eval` now archives the newest CSV per arm to `$OUTPUT_DIR/eval_{name}.csv` right after each eval (speedrun evals a single arm → no change needed)
- **Report STEM display verified (test-locked)**: base_eval's "STEM metric" = average CENTERED accuracy over the evaluated STEM benchmarks (base_eval.py:387), so the report's "STEM (centered acc)" label is accurate; STEM metric + STEM NLL tables + per-task breakdown + CORE-preference + N/A fallbacks + old-log parsing (no nll column) all covered by 31 checks in `/tmp/opencode/test_p1_stem_ratio_report.py`
- **Paper deviations documented** (`docs/paper_deviations.md`): 12 deviations with paper citations — search budget 35 vs 112 (64/32/16), guided sampling top_n = 3×M + Dirichlet exploration (paper: "sample M from top N", values unspecified), final selection 4-concentration×25K pool + 5K refinement (paper: predictor argmax over unspecified enumeration), LightGBM params (ours max_depth=3/min_leaf=3 vs paper 4/5; L1/L2 + early-stop-20 + val split aligned), cluster band vs fixed K=21, τ=0.9-on-normalized vs paper's Euclidean 1.5, random-baseline shortfall policy, SNR metric, 70/30 mix, scale, chars/4, eval cap — plus a positive audit (aligned: stella_en_400M_v5, FAISS spherical K-means K_init=1000, prune threshold 3.0, Dirichlet init on token counts, argmax selection semantics, Random=1/K, WSD anneal). CORRECTION: the earlier audit's claim that "1.5/3.0 are not in the paper text" was based on v1 — v2 §3.1 states BOTH (Euclidean 1.5 merge, 3.0 prune)

- **Stage-scoped fingerprints** (`runs/lib/stage_gate.sh` + `fingerprint.py --stage`): single global fingerprint meant a target-stage knob (MID_DEVICE_BATCH_SIZE) would archive multi-day search results. Now `.fingerprint_search` (Steps 1-3 products) and `.fingerprint_target` (Steps 4-8 products) are checked independently: search mismatch → archive all; target mismatch → archive only target products, search kept. File classification: search-only (proxy_runner/climb_pipeline/embedding_cluster/discovery/predictor/dirichlet/iterative_bootstrapper/cluster_merge/quality_filter/run_climb.py), target-only (target_runner/report_generator/prepare_shards/prepare_random_baseline/mix_general_data), everything else BOTH (conservative); diagnostics + get_model_info excluded. Params split by consuming stage; shared (stem_ratio, eval sets, dtype, data dirs) in both. `num_npu` removed from fingerprints (parallel shape only — pool may shrink/grow, remote executors later). Legacy single-`.fingerprint` dirs: `MIGRATE_LEGACY_FINGERPRINT=1` adopts unverified (one-time), else archives with hint. Guarded by `/tmp/opencode/test_stage_fingerprint.py` (44 checks) + updated `test_fingerprint_p1.py` (array parsing, NUM_NPU excluded-by-design)
- **d28 Step-6 OOM fix (round 2, root cause corrected)**: dbs=4 did NOT fix it (27.50 vs 27.58 GiB, same wall) — activations were never the dominant term: nanochat keeps **fp32 master weights + fp32 grads** (~13G static at d28/ws=8; GPT.Linear docstring "master weights stay fp32"; no set_default_dtype anywhere; embeddings bf16) despite bf16 *compute* (autocast/Linear-cast). Aligned to quadmix `run_stem_experiment.sh` (dev/dataset-schema, SAME nanochat-npu repo + SAME d28 ckpt, empirically OOM-free): `MID_DEVICE_BATCH_SIZE=1`, full NPU env block (`runs/lib/npu_env.sh`, sourced ONLY inside Step 6/7 subshells — unified memory + fusion passes; the 2026-08-26 device-0 blowup was the *parallel-search* form, single 8-rank torchrun is the proven-safe form), `--sample-every=-1` (default 500 fires at last_step → Engine.generate_batch() fragments NPU memory → optimizer OOM; quadmix af525ee). `--loader=flat` everywhere (proxy_runner + both runners' Step 6): zero-crop document packing, same data semantics across proxy/target/quadmix — search fingerprint changed by design → 2026-08-27 search archived & rerun (~1.5h, approved). ckpt meta does NOT record dtype (user_config = CLI args only; NANOCHAT_DTYPE is env) — tensor-dtype check on server is informational; bf16 conversion script deferred unless production wants dbs≥2
- **d28 Step-6 OOM fix (round 3, FINAL — root cause: optimizer grad stacking + step-0 val eval)**: 2026-08-28 speedrun (fresh search: flat semantics, K_ENHANCED=3, EMBEDDING_SAMPLE_SIZE=20000) reached Step 6 and dbs=1 OOM'd at the FIRST `optimizer.step()` — 22.24G alloc, 2.0G request refused, torch reserved 24.52G (= its ceiling; non-torch ~4.7G). Mechanism: `DistMuonAdamW.step()` Phase-1 stacks ALL Muon shape-groups' grads (optim.py:515-519) — every group gets a full `stacked_grads` copy (≈ all matrix grads, 5.25G fp32, dbs-INDEPENDENT) + the current group needs `grad_stack`+`stacked_grads` 2× largest group (~4G transient) → peak ≈ static 16.0 + 5.25 + 2.0 + comm ~1 ≈ 24.2G vs ~24.5G ceiling → <0.3G slack; any fragmentation kills the 2G contiguous request. The fragmentation source: `--eval-every` default 100 fires at step 0 (`0%100==0`) → 1280 val forwards before the first training step. quadmix passes `--eval-every=-1` explicitly (clean allocator, same stacking path × 390 steps, zero OOM) — that was the one arg diff all along (verified exhaustively: load-optimizer=1 default both sides, same ckpt/ws/T/total-batch/accum, flat-vs-bos loader is GPU-neutral at 2·B·T bytes; quadmix ran 07-24 pre-cecdf93 but that commit only changed LR reading + commented out muon torch.compile — stacking path identical). Fix: `--eval-every=-1` in speedrun Step 6, run_climbmix Step 6, AND `target_runner._build_mid_train_cmd`. dbs verdicts FINAL: dbs=8/4/2 die in forward (27.5/27.5/26.9G), dbs=1 dies at optimizer without the flag, dbs=1+eval-every=-1 = quadmix-proven. Old linear projection (dbs=1≈18.9/dbs=2≈21.8) refuted; check_ckpt_dtype.py now models the stacking term and computes Muon shape-groups from ckpt tensors. Proxy (d20) keeps val-bpb on (ample headroom, all 21 exps passed); target val-bpb is now off (quadmix also ran with it off). eval-every deliberately NOT in FP_TARGET_PARAMS (observability-only like sample-every; also avoids re-archiving the fresh climb_mixed)
- **d20 proxy → d28 target pipeline**: embedding cluster → Dirichlet search → LightGBM predictor → d28 target training
- **Paper-fidelity audit** (arXiv:2504.13161): stella_en_400M_v5 embeddings, FAISS spherical K-means (K_init=1000), prune threshold 3.0, Dirichlet init on cluster token counts, predictor-guided sampling, LightGBM features = mixture weights, WSD annealing semantics — all verified aligned
- **stella NaN fix on torch_npu**: `position_ids` buffer held uninitialized heap garbage → OOB RoPE → NaN; repaired via `_repair_stella_buffers` in `embedding_cluster.py`
- **embedding_cluster only**: fdc_labels removed entirely, single discovery strategy
- **STEM benchmark evaluation** (full benchmark set, no per-task cap): arc_easy, arc_challenge, mmlu_stem, gpqa_diamond, gsm8k_cot, math_cot_500
- **70% STEM + 30% ClimbMix mixing**: adaptive shard count (3-50 shards, calculated from STEM doc count), reverse-order download (shard 6542→6541→...) to avoid overlap with pretrain data (shards 0-999)
- **mix_general_data.py reuse**: proxy_runner and target_runner load scripts/mix_general_data.py via importlib, no code duplication
- **auto_detect_depth_info**: 3-tier fallback (GPTConfig from meta_*.json → _approx_scaling_params formula → DEPTH_INFO table)
- **NPU support**: device_type=npu, embedding_cluster tries torch_npu first with CPU fallback (192 threads)
- **Parallel proxy search**: --npu-per-exp 1 → 8 experiments concurrently on 8 NPUs
- **Token caps**: --proxy-target-tokens / --target-tokens (default 200M proxy / 1B target in production, 10M in speedrun; human-readable "2B/10M/500K" syntax) — caps per-experiment I/O and peak RAM (8 parallel read_texts)
- **Seeded data selection**: cluster docs randomly permuted (seed = exp_id + 42) before token-budget prefix cut
- **Val split hygiene**: last shard_*.parquet = held-out validation (train shards exclude val docs); DDP row-group sizing mirrors prepare_shards.py
- **Parallel-safe eval via private base dirs**: base_eval.py writes its CSV to `{NANOCHAT_BASE_DIR}/base_eval/mid_model_{step}.csv` — step-only, no model tag (confirmed on remote), so parallel evals used to overwrite each other and were serialized behind a global lock. Now each eval subprocess gets a private base dir (symlink farm: mid_checkpoints/tokenizer/eval_bundle/eval_stem → real shared data; private base_eval/ + report/) — no lock, evals fully parallel, CSV attribution unambiguous
- **Eval subsample cap (`--eval-max-per-task`)**: base_eval shuffles each task with fixed seed 1337 before truncating, so capped runs still score the same subset across experiments (comparable). Speedrun: 100/task; production: -1 (full sets). Recorded in meta.json and the run fingerprint
- **No duplicate in-training benchmark eval**: proxy/target mid_train now pass `--core-metric-every=-1` (the default fires all 28 benchmarks at last_step inside training — measured ~2h10m/exp on the speedrun, duplicating the external base_eval; val bpb stays on as the training signal)
- **mid_train resume marker (`.mid_train_ok`)**: weights-sha256 + model-tag + checkpoint presence; a crash/kill during eval resumes at eval only instead of retraining (speedrun 2026-08-27: ^C during iteration-2's first eval)
- **Subprocess heartbeat**: every 5min the runner prints stage elapsed + last log line (mid_train step / eval task progress) + log path at start — a 3h eval no longer looks like a hang
- **nanochat-npu integration**: all training via subprocess torchrun, --device-type npu
- **Self-contained scripts**: get_model_info.py and mix_general_data.py in climbmix/scripts/, no quadmix dependency
- **HF_ENDPOINT defaults to hf-mirror.com in runs/*.sh**: corporate proxy selectively rejects Python CONNECT tunnels to huggingface.co (curl + Python-to-mirror both fine); mirror serves identical bytes; covers ClimbMix shards + eval_stem.zip; override with the env var
- **Fingerprint gap closure**: all semantic knobs previously hardcoded in `runs/*.sh` (proxy/target lr-scale/warmup/warmdown, K_init, filter-method, prune-threshold, merge-distance, embedding-model, search num-iterations, mid/eval device-batch-size, core-metric-every, NANOCHAT_DTYPE) are now `${VAR:-default}` shell vars passed to BOTH the invocation and the fingerprint `--param` list — changing any of them archives the stale output dir instead of silently resuming. `/tmp/opencode/test_fingerprint_p1.py` guards this: it parses the scripts and fails if a semantic flag consumes a `$VAR` missing from the fingerprint
- **Cluster-count band (pool-adaptive K)**: `K_final = clamp(natural_K(τ), K_enhanced=3, K_max=15)`. Floor = permissive safety bound (follows the pool's natural structure down to very few clusters; set to the paper's fixed K_enhanced, e.g. 10/20, for paper-faithful semantics), cap = search-budget bound (35 configs), τ=0.9 merge legality on unit-normalized stella embeddings (cos ~0.6). Inside the band the distance guard NEVER force-merges semantically distinct clusters (the failure mode where distinct subtopics share one weight and the true optimum becomes inexpressible); beyond the cap, closest-pair forced merges (logged) keep heterogeneous pools within budget. The paper merges to a fixed K_enhanced (20 in the main experiment) regardless of distance — the band is a deliberate deviation. Diagnostics: every run PRINTS a merge-profile block after clustering (sampled distance curve, natural_K at τ∈{0.7..1.2}, elbow, and automatic tuning advice: guard-stop → OK / forced → raise cap? / floor-stop → coarser pool, stricter τ?) and writes `merge_profile.json` (same data + advice list) — the audit trail for K decisions per data pool
- **Pool-keyed stable caches**: embeddings + K-means live in `cache/embeddings/<sha256(pool manifest, model, truncate_len)[:12]>/` (NOT inside the fingerprinted output dir) — changing K_enhanced/K_max/τ/prune re-runs only prune+merge (seconds) instead of re-embedding the pool (hours). flock serializes concurrent runs over the same pool; kmeans cache keyed by K_init. Run-level artifacts (cluster_cache.npz, merge_profile.json) stay in the output dir and archive normally
- **`--num-iterations` validation** (run_climb.py): defaults to `len(configs_per_iter)`; explicit value that doesn't match the per-iteration list length is a `parser.error` (previously: extra iterations silently skipped)
- **search_state.json records `n_clusters`**: on resume, if the cluster count changed (recomputed cluster cache, edited data, or a run_climb.py invocation bypassing the shell fingerprint), the stale state is discarded with a warning instead of mixing wrong-dimension weight vectors into the predictor; old-format states (no field) are checked via weights length, pending-only states via pending configs
- **Crash resume (A+B+C+D)**: fingerprint auto-reset (code/param change → archive stale output dir), shard-level embedding resume (memmap + per-worker progress ledgers), iteration-level search state (`search_state.json`, atomic, with pending-iteration configs), experiment-level reuse (`exp_XXXX/meta.json` rc=0/0 + weight match, globally unique exp ids), predictor refit on resume (search continues paper-faithfully incl. full-design-space selection), atomic writes + `.done` markers for shards/mix/sampled/cluster caches, per-target `.done` markers with partial-checkpoint cleanup. EXP_NAME isolates experiments (output dir + proxy/target tags)

## Resolved Design Decisions

- **d20 ≈ CLIMB proxy**: scaling_params=435.2M, 1000 iterations, 8 parallel experiments (1 NPU each)
- **d28 target**: auto-detected from meta_*.json checkpoint, not hardcoded in DEPTH_INFO
- **Cluster-count band [3, 15] with τ=0.9**: floor 3 = permissive safety bound (coarse pools keep their natural structure instead of being pinned to 10; paper-faithful = set K_ENHANCED to the paper's fixed K_enhanced — 20 in its main experiment); cap 15 = search-budget bound for our 35-config budget (paper used 112); τ=0.9 (~cos 0.6, on L2-normalized embeddings) rejects merges of semantically distinct clusters inside the band. Paper v2 §3.1 states its own merge threshold (Euclidean 1.5, unnormalized-space — not directly comparable to ours; see docs/paper_deviations.md D5/D6); see the printed merge-profile diagnostics + merge_profile.json per pool. **REVISED 2026-09-04 (prod1 full pool, 116M docs / ~92B tokens)**: extreme skew (one direction-basin holds 99.86% of docs at K=3) makes natural_K(τ) unstable (0.7→49 / 0.8→4 / 0.9→≤3) and the floor degenerate (search collapses to single effective parameter f(w_C0); random arm 383M vs CLIMB ~1B tokens breaks comparability). Decision: fixed K_ENHANCED=14 from the merge-distance ELBOW (largest jump 0.0816 → 12 macro clusters ~7.6B tokens each + 2 dust clusters, 0.07% docs) — the paper's fixed-K semantics on our pool, under the band cap (search budget 35 vs paper 112) and just below the paper's own granularity (main 21, ablations 15/30, ClimbingLab 20). kmeans cache unaffected (keyed on K_init=1000 + pool; kmeans_K1000.npz holds FULL-POOL labels, so a K_ENHANCED change re-runs only prune+merge — seconds)
- **ClimbMix adaptive shards**: calc_climbmix_count(stem_docs, ratio) → ceil(stem_docs * 0.3/0.7 / 500000), clamped [3, 50] — not all 400B
- **Reverse-order download**: shards from MAX_SHARD (6542) backwards, avoids overlap with pretrain shards 0-999
- **Stream-based mixing**: stream_texts_uniform + endless_generator, memory-efficient (no full List[str] loading)
- **STEM eval metric**: --eval-benchmarks=stem → CSV "STEM" row parsed as stem_metric; SNR-weighted acc+NLL z-score (see docs/scoring_metric_design.md)
- **Proxy 并行 k 选择指南** (`docs/parallel_k_selection.md`, 2026-08-28): 生产搜索"每实验卡数 k"的决策模型与闭式规则。实测锚点: d20@1卡 60s/步 (speedrun), d28@8卡 18.2s/步 (run-4, mfu 16.71) → 加速比近线性 S(2)≈1.85/S(4)≈3.5/S(8)≈6.5; 生产单实验 T(k)=18.6/10.0/5.3/2.8h @ k=1/2/4/8 (scaling 税 +7.5%/13%/21%), eval 全量 ≈1.9h@1卡 (95% 是 gsm8k_cot+math_cot_500 生成任务)。核心结论: 吞吐 ≈A/T(1) 与 k 无关, k 只决定切片 (波长/单轮样本数/节点装箱); 迭代屏障空转 (30-80%) >> scaling 税 (7-21%) → 设计越小最优 k 越大; {nᵢ} 可自由定制时最优解 = 先定 k 再取 nᵢ 全为 C=⌊A/k⌋ 的整数倍 → Wall=T(k)·N/C 零空转。三步法: 节点装箱算 C(k) → λ_max(波长上限=不可反悔合同长度: 灾难最大损失 A·T(k) 卡时/纠偏节奏) 定 k 下限、节点形状定上限 → nᵢ 取 C 整数倍。当前决策: 首个生产 run λ_max≈5h → k=4 (speedrun 已全绿但生产仍是规模跃迁: 100B 池全量 embedding 走 E 里程碑/1000 步全量 eval/RemoteExecutor 首战, 灾难上限 ~74 卡时@32卡池), 全绿后放宽 λ_max≈10h → k=2; k 全程固定 (分数可比性), 池波动由队列派发吸收。RemoteExecutor M4 (mixed-fleet 调度) 应实现该三步法为 launch planner
- **PROXY_TARGET_TOKENS=400M（单遍校准，2026-08-28）**: epoch = 训练消耗 ÷ 混合数据量（loader 循环数据，无守卫无警告）。生产 proxy 消耗 524M（1000 步 × 524,288）；200M cap → 混合 286M → epoch≈1.8 静默重复（实证：speedrun proxy ≈1.8，run-4 target 日志 epoch:4）。400M → 混合 571M ≥ 524M → 单遍 ✓。论文 800M 是其 proxy 消耗量（~394 步 × 2M batch），非采样上限——我们的消耗 524M = 论文 65%（已记录偏差 scoring_metric_design.md §12.3），配 800M 等效 cap 反而会采 ~276M 永不被训练的 tokens。run_climbmix.sh 默认已改 400M；显式重复率守卫（告警/拒绝 epoch>1）为后续可选项。speedrun 保持 10M 不变（口径已锁定）
- **TARGET_STEPS=2000 / TARGET_TOKENS=2B（2026-09-04，用户决策）**: prod1 直接采用 2000 步而非 §12.6 的"1000 步 + 视显著性升级"路径——预防性消除"1B 量级下混合差异清不出噪声"的风险（论文 proxy 在 350M 模型 ~800M tokens 可测出差异；我们 1.5B 模型 + 6 任务全量 eval + SNR z-score，2000 步 ≈ 2B 消耗 = 论文 proxy 的 2.5×）。TARGET_TOKENS 同步 1B→2B 维持单遍（消耗 2000 × ~1M ≈ 2B ≤ 混合 2B/0.7 ≈ 2.86B → 0.7 epoch）；random 臂 1/14 配额尘埃簇短缺后有效 STEM ≈ 1.76B vs CLIMB 2B（12% 短缺，与 1B 时同比例）。墙钟代价：Step 6 两臂 10.1h/臂（18.2s/步 × 2000）≈ 20.2h + eval ~3h。proxy 搜索不变（1000 步）；两参数在 FP_TARGET_PARAMS（target 指纹），不影响 search 产物
- **Continual pre-training**: all stages anneal from base checkpoint, lr_scale=1.0, warmup=0.0, warmdown=0.9
- **Random baseline = equal cluster weights (paper App. C.1)**: "each cluster is assigned an equal and uniform weight" — NOT a doc-uniform draw (that would weight clusters by natural size, i.e. feed the pool's skew). `prepare_random_baseline.py` now runs the CLIMB arm's own selector (`select_data_by_mixture`) with α_k=1/K, the same `--target-tokens` cap, and the same shortfall policy (cluster < quota → take all docs, no duplication, no redistribution — data_selector's existing behavior), so both arms degrade identically and stay comparable. The paper documents no small-cluster policy and needs none (800B tokens / 21 clusters / 40B budget → ~1.9B-token quota per cluster, only a <0.24% cluster would fall short); our smaller pools can hit shortfalls — loudly logged + recorded in `.done` (planned vs effective weights, shortfall cluster ids). Labels come from `cluster_cache.npz` with a length-mismatch hard fail (pool changed since cache); token counts from the pool's precomputed char-count column (no full-text scan of the pool)

## Remaining

- **prod4 赢家选择机制判读 + 改进方向（2026-09-17 记档；全部基于 prod4 单轮证据——111 实测点/本池/修正后评分版，非普适结论；触发 = L0/L1a 裁决落地时逐项对照）**: ① **判读事实**: prod4 终选 `Selection mode: predictor_design_space`（search.log:51585）——首次在"有信号"路径外发插值点（prod1 无信号角落翻车→加 no-signal 守卫；prod3 终选落在实测配置 cfg29，编号即 best-measured 路径证据；prod4 守卫放行）。末轮预测器 val R²=0.32 / val Spearman=0.50 / 在线回测 ρ=0.421（n=19，CI 宽）。赢家 = C10 .4737 + C5 .3540 + C12 .0641，**12/15 簇钉在 0.9% 地板**（0.01 钳位+重归一侧产物，2026-09-15 采样器权重地板含终选 refinement 路径；地板质量 ~10.8% 是防饿死政策压舱，非搜索输出）。② **问题清单**: (a) 朴素 argmin 的赢家诅咒——**2026-08-29 smoke_search 合成实验已量化**（argmin 比最佳实测差 0.2-0.3 utility；机制 = 树不外推 + 引导带窄化训练分布 + 宽池角落 leaf 值被 argmin 利用；当时决策保论文语义 D3，"best-of {argmin, best-measured}" 与 "+1 proxy 实测 argmin" 两变体被否——否决理由 = 搜索舰队尾部串行 ~10h）；(b) 真实信号中等（排序相关 0.42-0.50）撑不起"最优点"精度，只撑得起"好区域"读法；(c) 地板堆积是 prod3 病灶的结构回声（当年近零簇饿死推理示范；评分已修但形状风险独立存在）；(d) 赢家是唯一未在任何尺度实测过就外发的配置；(e) 111 点/15 维稀疏（被三簇塌缩部分缓解，有效维度 ~3）。③ **L0 诊断链**（若 climb 输 random3b）: 第一嫌疑 = 地板堆积饿死簇（对照 prod3 语义地图，查 C6/C8/C9 是否再被钉地板），第二 = 评分回退（对照 w 表与 [Scoring] WARNING），第三 = 插值噪声本身；L1a（cfg11/cfg25 实测代表）直接裁决"插值 vs 实测"。④ **改进方向**（带触发条件）: 1. **选择时 claim 报告**（P1，零行为变化，下次搜索即做）: 终选打印赢家预测分 vs 最佳实测分（claimed gain）+ 到最近实测配置的 L1 距离 + 地板簇数与质量份额；claimed gain ≤ 任务噪声 → 降级 best-measured（no-signal 守卫推广为 no-claim 守卫）。2. **赢家 proxy 复测**（P1，重议 2026-08-29 否决——上下文已变：臂阶段独立作业，无舰队尾部串行）: 搜索收官后、L1b/L2 预算承诺前，对赢家配置跑一次独立 proxy 实测（成本 = 一个 proxy 配置）；实测分回归到 best-measured 水平 → 后续大预算轮降级用 best-measured。3. **不确定性感知获取**（P2，行为变化，触发 = L1a 证明插值输给实测）: LightGBM 种子集成（~5 seeds → 预测 std）+ LCB（pred+λσ）替代朴素 argmin，λ=0 退化为现行为；动 D3 论文语义需 paper_deviations 脚注。4. **地板质量审计**（P2）: 选择时报告 floor mass 份额；可选地板质量上限/熵约束，把"防饿死保险"与"搜索输出"分开记账。⑤ **论文核对（2026-09-17, arXiv:2504.13161v2 全文+附录）**: 该问题论文未讨论——全文无 uncertainty/extrapolation 字样，终选 = Eq.4 朴素 argmin（"selects the best configuration predicted by the final predictor"），唯一 overfitting 措施 = LightGBM 正则（§3.1：L1/L2+早停+深度 4+叶样本 5——治拟合层面过拟合，不治选择层面的赢家诅咒）；附录 A Limitations 仅列 proxy 成本与 MMLU 粒度；D.8 把稀疏赢家当**特性**（"we intentionally bias towards sparse weights... amplifies important clusters"）——与我们的 prod3 病灶史语境相反（论文无饿死风险讨论）；D.10 报 94% held-out Spearman（3 个 MC 任务验证损失、低噪声口径）vs 我们 0.42-0.50（6 任务 acc+NLL 含生成式 CoT、高噪声）——差距在任务面不在实现，且带内排序质量同样不背书 argmin 在探索带外的外推；CLIMB-Best@N（§5）是论文内最接近的对照但非同尺寸（随机点+目标尺度模型挑最优，非 proxy 实测最优点）。结论：上述四条改进方向是填论文空白，非重复造轮子。⑥ **实测画像补全（2026-09-17 上午，search_state 直读）**: 最佳实测 = **cfg25 1.2550**（其次 cfg72 1.1297 / cfg60 1.1134 / cfg92 1.0467；prod3 旧赢家 cfg29 重排后 0.9003，修正评分裁决自洽）；赢家**外推半径 L1=0.65**（最近邻 cfg72，cfg25 本身 0.87）——不是任何实测点的精修，是大步外推；赢家邻域实测分 1.05-1.13 全部低于最佳实测 0.12-0.40，预测器"赢家 > 1.2550"的 claim 无邻域证据支撑 = 赢家诅咒典型签名 → 改进 #2（赢家 proxy 复测）优先级上调。L1a 构成待定：cfg11（重排后 0.5156 中游、饮食多样 C0 .40/C11 .16/C5 .14/C13 .14）入选理由疑为重排前旧排名残留，候选替换为 cfg72（赢家最近邻 1.1297——"插值是否胜过自身邻域"的直接判据）；cfg25（C10 .78 + C0 .12 + C12 .07 + C8 .01，与赢家共享 C10 主导）为决定性对照不变。**[定案 2026-09-17: L1a = cfg25 + cfg72（用户拍板）；cfg11 搁置；权重已落 /home/ma-user/work/tmp/cfg{25,72}_weights.json；cfg72 饮食 = C10 .66 + C11 .14 + C5 .08（与赢家/cfg25 同处 C10 热区，引导采样带 0.01 地板的产物），1.1297——L1a 由此成为"热区内部"对照：同区域三个点（插值/实测冠军/最近邻）互相检验，控制了区域变量、专测"点"的优劣]**

- **prod3 run4 战后修清单（2026-09-15 汇总；run 进行中记录，结束后实施。用户两条硬性要求：1.5B 训练/验证必须并行、以后执行一律走版本化 .sh 禁止裸命令）**:
  1. **两臂并行 + 预发恢复（P0，用户点名）**: run4 两臂串行多付 ~5.5h——`run_climbmix.sh:698-699` `run_arm` random→climb 逐个阻塞，random ~15:25 训完才轮到 climb。根因链：TODO ① 预发竞速未修 → 本次 `DISPATCH_RANDOM_ARM=0` 规避 → 串行暴露。修法 A：预发修复（`load_launch_env` 对 launch_env/remote_config 加有界等待 ~120s，或把两文件写入提前到指纹门前）→ random 恢复与 14h 搜索重叠（省的不只 Step 6 串行，是整个 random 臂墙钟）；修法 B：Step 6+7 两臂 dispatch 改并行子壳 + wait，预发失效也不串行。A+B 都做。本次已验证 flock/`.done` 幂等语义支持手动并行（第二 dispatcher 阻塞等待后干净 no-op，`dispatch_target_arm.py:538/:541`），机制可直接复用。
  2. **dispatch HF_ENDPOINT 断链（P0）**: `dispatch_target_arm.py:617` mix 子进程 `dict(os.environ)` 不带 launch_env 的 HF_ENDPOINT → bare-shell 拉起撞公司代理 503（本次 random 臂 mix 卡死过夜，卡在 ClimbMix shard_06522/06528 下载，urllib3 Tunnel 503 循环）。一行修：`env_mix.setdefault("HF_ENDPOINT", launch_env.get("HF_ENDPOINT") or "https://hf-mirror.com")`；彻底解：`nanochat/dataset.py:29` 缺省改 hf-mirror.com（动 nanochat-npu 会随 worker tar 发布，挑时机）。注意 dispatch_target_arm.py 未被 fingerprint.py 分类 → 改它移双指纹。**[已修 2026-09-16 晚: dispatch 侧 `mix_subprocess_env()` + arm_engine.sh 顶部 export（同优先级链），见 Completed 同日条目；nanochat dataset.py 缺省改动仍待时机]**
  3. **单线程热点清理（P0，用户点名：192 vCPU / 1.5TB 不许再单线程）**: 从零发射 Step 1 的 `compute_cluster_quality` + `diagnose_prune_profile`（cluster_merge.py:34 起）逐簇布尔掩码扫 116M docs，单线程烧 43 分钟（实测 top 1 核 99%、其余 191 核闲）。改 `np.bincount(weights=...)`/矩阵化 → 分钟级。顺带审计 from-zero 路径其它单线程段（embedding 缓存逐块校验、balanced merge 前置统计）；faiss/OMP 共享 libgomp 竞态的 `_blas_single_thread` 再断言（embedding_cluster.py:1277 docstring 有档案）验证仍生效。
  4. **发射/操作规程版本化（P0，用户点名）**: 本次三类"忘全局配置"翻车在案：(a) standalone 远端 API 脚本忘加 `climbmix-ma` PYTHONPATH（ModuleNotFoundError ×2）→ 修法：`backends.py:_load_from_module_spec`（:101）自动把 `<repo>/climbmix-ma` append 到 sys.path（目录存在即加），整类问题消灭；(b) 手动 dispatch 忘 HF_ENDPOINT（→ 第 2 条）；(c) 发射 env（EMBEDDING_CACHE_DIR=OBS 池缓存 `/l00916525/prod/climbmix/cache/embeddings`、REMOTE_LOCAL_PARALLEL=0、TARGET_ARM_NODES=4、TARGET_TOKENS=3B、CONFIGS_PER_ITER=20,20,10、STEM_RATIO=0.7 显式钉死）只活在临时脚本 `~/work/tmp/launch_prod3_run4.sh` → 落进 runs/run_experiment.sh EDIT 块 + 注释（TODO ② 的 EMBEDDING_CACHE_DIR 统一正式落地），服务器发射一律 `bash <版本化 launch_*.sh>`，探针/看护/清理脚本同样入库或归档，不再拼裸命令。
  5. **日志盲飞（P1）**: nohup 块缓冲让 Step 1 静默 45 分钟（短打印憋在 8KB 缓冲，误判"卡死"两次）。修法：run_climbmix.sh / runs/*.sh + runs/lib/*.sh 对所有 python 调用统一 `export PYTHONUNBUFFERED=1`（或 `python3 -u`；runs/*.sh 不进指纹，随时可改）。附带：py-spy 在容器内 ptrace EPERM → `~/.local/bin/py-spy dump --pid <> --nonblocking`（pip 装完不在 PATH）。
  6. **ClimbMix 分片预取（P2）**: Step 5 现场下 ~13 片 × ~91MB 走 mirror 耗 ~45 分钟（实测 ~280KB/s）；改搜索阶段后台预取，或一次性打进 OBS assets——一次性成本归零。
  7. **既有项（09-12/09-14 已记，只点名）**: 串行分支 per-exp catch（proxy_runner.py:349-355）；CHAR_TO_TOKEN_EST 死变量清除；heredoc PYEOF lint；REMOTE_MAX_JOBS 改名；climbmix-ma 两文件待 commit。
  8. **臂混料上传后本地清理（P1, prod4 磁盘事故新增）**: proxy 实验上传成功后自动清理本地 mixture（proxy_runner.py:183），但臂路径没有——每臂 ~18G mixed + ~13G shards 常驻本地，多臂并行直接吃穿 /work（prod4 2026-09-16: 3×6B ≈ 100G，盘到 100%）。修法：arm_engine/dispatch 臂产物上传成功后删本地 `*_mixed/shard_*.parquet`（保留 .done 台账）；发射前加磁盘预算检查（preflight: 各臂 shards+mixed 估算 vs df 余量，参考 20B 方案当时做过的 ~720G 手工检查）。**[已修 2026-09-16 晚: `check_disk_budget.py` preflight（4.5+6.5 GB/B×1.15，free≥need×2+50G）+ `clean_derived_data.py` 清理（arm_engine 自动挂接 + 双守卫 + 默认 dry-run CLI），见 Completed 同日条目]**附带：**8 节点臂（ws=64）对 d28 结构性不可行**已实证（Muon optim.py:516 stacked_grads 随 ws 线性放大，64 rank 需 4GiB / 29.5GB HBM——见 Completed 2026-09-16 判读），臂节点上限 4（ws=32），NPU 扩容对臂无效、只对搜索（NPU_PER_EXP/REMOTE_MAX_JOBS）有效。**[2026-09-16 晚更新: ws=64 墙已由 nanochat-npu `45c3c1a` 拆除（小簇 Muon 路径：逐参数 all_reduce+轮转所有权+owner 广播；`8d980f3` 三锚点测试 12/12 逐位——单卡未触碰参考类喂预平均梯度为独立语义锚）；16 节点（ws=128）同为零改动可行（2²⁰/(128×4096)=2、2048%128=0、98304%128=0、d28 全参数组 n≈28<128 全走小簇路径）。约束本质是 2 幂量子而非实现限制，见 #9；NPU 扩容对臂重新有效]**
  9. **非 2 幂节点数支持（P3, 2026-09-16 定案记档；触发条件：集群常态性缺 2 幂空闲节点才做）**: 节点数 2 幂约束 = 三重墙，非"2²⁰ 神圣"：① 梯度累积必须整数 → total_batch 必须为 ws×micro_bs×4096 的倍数（nanochat base_train.py:446 断言），且 :424-446 对不可整除值会**自动向上 bump 并打 warning**——10 节点不显式干预 = 偷偷 +25% batch；② batch 非孤立旋钮，自动连锁三件套（base_train.py:286-300, 371）：peak LR ∝ √(B/B_REF)、weight decay ∝ √(B/B_REF)×(D_REF/D)、steps = target_tokens//B——"只改 batch"实为同时改 batch/LR/wd/步数，与 2 幂基线不再严格同配方（prod3 执行面对称性曾验证到 0.09%，勿轻易弃）；③ AdamW reduce_scatter 断言 shape[0]%ws==0（optim.py `_reduce_adamw`；ws=80 时 lm_head 98304%80≠0 直接崩，修需行填充新路径 = 又一轮未验证优化器代码）。可行量子实测：10 节点最优 = 显式 983,040 + micro_bs=3（−6.25%）；12/14 节点为 −25%/+12.5%/−12.5%/+31%，全是大步。若做：total_batch 显式取整（拒绝 auto-bump）+ micro_bs 覆盖通道 + AdamW 行填充（~30 行 + 三锚点测试扩展）+ 可比性脚注；验证成本约半天。**当前由 16 节点零成本替代**。
  10. **32/64 节点外推墙 + NPU 扩展经济学（P3, 2026-09-16 记档；触发条件：20B 终轮设计启动时必读）**: 恒等式 total_batch=2²⁰ = 节点×8×micro×4096 在 d28 现配方下的外推：32 节点(ws=256)→micro=1（整数下限）、64 节点→micro=0.5（**非整数，结构性死区**）。且 32 节点在到达前先撞三堵墙：① dispatch 守卫的不可整除分支（2²⁰ % (32×8×4096=2²¹)≠0，见 `f1e7106` device-batch identity guard 的警告路径）；② Muon 小簇路径 NS 计算期空转份额随 ws 恶化（每组 n≈28 个参数由 28/256 个 rank 计算）；③ **通信:计算比**：每步 ~570 个固定 all_reduce 轮次（全部 Muon 参数 + AdamW 小参数），节点翻倍 → 每 rank 计算量减半而通信轮次不减 → 比值翻倍。**经济学实证锚点**：mfu=11.4%（16 节点，micro=1【2026-09-17 勘误：原记 micro=2 系标错——2e58bdab 启动命令 --device-batch-size=1 实锤；db≥2 从未在任何 ws 跑成，见同日判决条目】）；prod3 4 节点(micro 标签 8，2026-09-17 降级待核：~15G 静态 + db=8 前向在 29.49G 卡上算术不成立，7.3s/step 与 db=1/GA=16 同样吻合) ~7.3s/step vs 16 节点 2.1s/step = 4× 节点换 3.5× 墙钟（递减）；prod4 8 节点中间点已落地（micro=1、3.35s/step、mfu 14.4%）——8→16 加速比 1.58× < 1.8×，**32 节点现配方判死（2026-09-17 判决生效）**。**根因框架**：micro 随节点数缩小是"用每卡效率换墙钟"（total_batch 恒定=每步总 token 恒定，更多卡分摊），"+"batch size 提速"的经典杠杆存在但被 A/B 实验的配方锁定（LR∝√B/wd∝√B/steps=tokens/B 全链在 total_batch 上，臂间可比性要求配方冻结）——**32 节点的正确打开方式不是改节点数而是 20B 轮重新设计 total_batch**（如 2²² 时 32 节点 micro=2 复活，同时吃回大 batch 的吞吐红利），届时需联动评估 critical batch size（~3B 模型的 2²⁰≈1M tokens/step 已接近临界，盲目翻倍损样本效率）+ LR 链重定标 + 与 prod3/prod4 基线的跨配方脚注。
  11. **NPU 利用率/mfu 提升（P2, 2026-09-16 记档；用户定性"利用好 NPU 是关键问题"。触发：fleet 裁决落地后启动，20B 设计前出结论）**: 实测锚点 mfu=11.4%（16 节点 ws=128、micro=1【2026-09-17 勘误，同 #10】、dt≈2.1s/step；8 节点 micro=1 对照点已落地：3.35s/step、mfu 14.4%）——88% 的每卡墙钟不在 fwd/bwd FLOPs 里。**第一步是测量不是优化**：单节点 20-step 计时探针（fwd/bwd、optimizer.step()、dataloader 分段计时）出时间饼图，先定位大头再决定投入。候选杠杆按预期性价比排序：① **优化器通信合并（便宜，预期显著）**：小簇路径每步 ~570 逐参数 all_reduce + ~570 逐参数 broadcast（~20 形状组 × ~28 层）+ AdamW 小参数逐个 all_reduce——同形状组内 n 个参数可 flat-concat 成一个缓冲做 **1 次** all_reduce/broadcast（无 stacked 路径 ws×shape 的 padding 内存代价，这正是小簇路径当年避开的；~50 行 + 三锚点测试扩展，集合通信启动次数 ~1140→~40）；② **NS 空转占比实测后决策**：每组 28 参数由 28/128 rank 计算是所有权分片在 n<ws 下的结构性下限，但代价大小取决于 NS 时间占 step 的比例（探针测出后才知道值不值得动，如减 ns_steps 或探索跨组流水）；③ micro-kernel 项与 ws-通信项分离（2026-09-17 实证：同为 micro=1，8 节点 mfu 14.4% > 16 节点 11.4%——同全局步下小 ws 集合通信更便宜、固定 ~570 轮通信被更多每卡计算摊薄 → 通信是主要项、指向①；micro-kernel 项仍无证据——db≥2 从未跑成，见同日判决条目）；④ 昂贵路线先调研后投入：torch.compile/torchair 图模式（910B 支持性未知）、fp8（仓库 fp8.py 是 H100 向，910B 大概率不可用）。**与 #10 联动**：total_batch 翻倍是最大结构性杠杆，但其代价与收益评估归 #10 的 20B 配方设计；本项负责配方不动前提下的工程性回收。验收：测量出饼图后定 mfu 目标（粗基准：同级模型大 batch H100 常态 40-60%，小 batch + 自定义优化器区间低得多；11.4→15%+ 即值得，具体以饼图为准）。

- **prod3 发射线诊断记录（2026-09-12, 服务器发射实证）——四项遗留**: ① **dispatch 预发竞速（P1, 未修）**: `runs/run_experiment.sh` 的 setsid 预发块 (exec 主脚本前) 预发 random 臂，而 `launch_env.json`/`remote_config.json` 由 `run_climbmix.sh:400/:439` 的 heredoc 在指纹计算（~10-30s）之后才写 → dispatch 的文件检查（`dispatch_target_arm.py:91-103/:492-496`）必然先到先死（"launch_env.json not found"；remote 模式下 remote_config.json 同窗口），"提前发车"优化从未生效过；且从零发射时 dispatch_random.log 先落进空 output dir → `stage_gate.sh:212` 把它当非空无指纹目录归档 orphan（无害噪音）。修法任选：`load_launch_env` 加有界等待（poll 两文件出现，~120s）；或把两文件写入提前到指纹门之前。注意 `dispatch_target_arm.py` 未被 fingerprint.py 分类 → BOTH-stages 保守默认，改动移双指纹，勿在 prod3 运行期间做。② **池级嵌入缓存本地/OBS 双副本陷阱（P1）**: 本地 `cache/embeddings/<key>/` 从未发布过内容（仅 .embed.lock），真正的 sharded-v1 全池缓存（manifest + block_u00xx + kmeans_K1000.npz，~40h 投资）在 OBS 挂载的远端 cache 路径（各历史 run 的 search.log "Pool-level embedding/kmeans cache" 行有实录）——本地发射不把 `EMBEDDING_CACHE_DIR` 指向该路径必 miss → 全池重嵌 ~40h。prod3 本次救场 = 继承 prod2 归档的 `cluster_cache.npz` + `cluster_info_cache.json` 双文件（run 级缓存命中，跳过 embed+kmeans）+ 预写同命令指纹（search `7f61077ec4003a9d` / target `29234e7ccc6ce5df`）防 orphan 归档。待办：发射线统一带 OBS 侧 `EMBEDDING_CACHE_DIR` 并写进 runs 注释/docs。③ **remote 身份块易漏（P2, 操作性）**: `REMOTE_ENABLED` 默认 0 且无自动探测/警告，漏传时静默退化为本地串行（REMOTE_MAX_JOBS/TARGET_ARM_NODES/ADAPTIVE_CONFIGS 全部失效，唯一线索是 banner "npu_per_exp=8 (1 parallel)" 与 remote_config.json 缺失）；prod3 已按 prod2 remote_config.json 重建远端 env（服务器 `~/work/tmp/prod3_remote_env.sh`: backend=modelarts + climbmix_ma:create_backend + flavor + 四 asset_mounts，具体值见该文件/prod2 归档，不进 repo）。可考虑：REMOTE_ENABLED 未置 1 但检测到 REMOTE_BACKEND≠mock 或 TARGET_ARM_NODES>1 时大声警告。④ **heredoc python 无语法门（44d9c4e 回归教训，已修 bf20cc2）**: PYEOF 块内缩进错误杀死之后每一次发射，`bash -n` 与 LAUNCH=0 干跑都不覆盖 heredoc 内 python → 建议 `scripts/diagnostics/` 加 heredoc lint（抽取 runs/*.sh 全部 `<<'PYEOF'` 块逐个 py_compile），与 check_repo_secrets.py 并列 push 前例行。
- **按簇 token 估算失准 → single-pass 守卫误杀 + CHAR_TO_TOKEN_EST 是死变量（2026-09-12, prod3 iter-1 实证, P1, 仅战后修）** **[根因更正 2026-09-14: 本条的"按簇系统性偏差"假设不成立——探针证明元数据 char 列 100% 精确、按簇标签对齐；实测漂移的真根因是 read_texts 缓存投毒（见 Completed 2026-09-14 条目），守卫按设计正确杀掉了错文档混料。CHAR_TO_TOKEN_EST 死变量结论仍有效]**: Exp 22/23 实测混料池 427M/475M tokens vs 规划消耗 639.6M（610 步 × 1,048,576）= 1.50/1.35 epochs > 1.02 上限 → 守卫杀掉；同批 Exp 18/21 实测 1,137M/1,241M（0.56/0.52 epoch）冗余交付。机制：选择侧按元数据估算 tokens（`token_estimate.py` chars/4 硬编码，DEFAULT_CHARS_PER_TOKEN=4）填 α_k × STEM 预算配额，守卫侧（`single_pass.measure_train_tokens`）实测混料分片真实 text chars/4——名义同口径，但实测交付/规划在 0.5×-1.4× 间按配置漂移，指向元数据 char 列与实际 text 的**按簇系统性偏差**（K=15 balanced 各簇 3.7-7B tokens，单簇配额打穿的 dust 短缺难以解释如此幅度）。影响：被杀配置 = 未测点（NaN，predictor 训练正确丢弃，`_compute_scores` 设计）；自适应池 e_i+4 部分吸收。修法（搜索语义 → 指纹变 → 归档在跑战役，**必须等 prod3 结束**）：按簇校准估算因子 / 选择时实测 / 池尺寸加安全边际。量化入口：`grep "single-pass violation" result/prod3_current/search.log` 数 iter-1 损失。附带发现：发射命令传的 `CHAR_TO_TOKEN_EST=0.84` **全仓无代码读取**（死变量）——要么接线（注意进指纹）要么从发射命令与文档删除防误导。
- **REMOTE_MAX_JOBS 命名语义澄清（用户反馈 2026-09-12, P3, 战后做）**: 名字像总并发上限，实际只限远端在飞；`REMOTE_LOCAL_PARALLEL=1` 时总并发 = REMOTE_MAX_JOBS + 本地整槽（prod3: 10+1=11 slots）。建议改名（如 REMOTE_MAX_INFLIGHT_JOBS）或至少 `run_climbmix.sh:238` 与 runs/run_experiment.sh EDIT 块注释写明。`remote_executor.py` 同为未分类 → BOTH-stages，代码改动移双指纹；纯 runs/*.sh 注释不进指纹、安全。附带观察：远端作业乱序提交 = 混料线程池（REMOTE_MAX_PREP）完成顺序 + 守卫失败者补位，正确性无关，无需修。
- **prod3 无人认领的 warm-start 种子（运维记录，待认领/删除）**: `result/prod3_current` 曾含 2026-09-11 14:37 注入的 history_seed（源 prod2_k15bal，30 点全测，`inject_history.py` 产物；`runs/run_extend_experiment.sh` EDIT 块默认 EXP_NAME=prod3）——用户确认 prod3 计划为全新实验，已整目录归档至服务器 `~/work/tmp/prod3_warmstart_seed_*`；若为另一会话刻意准备请认领（mv 回去 + CONFIGS_PER_ITER 第 1 槽 = 历史点数），否则可删。

- **RemoteExecutor M2 DONE (2026-08-28, code complete; M1 survey pending)**: production mixed-fleet execution is fully coded and locally simulated. Architecture: local host = scheduler + mixer (cluster labels + STEM pool live here); OBS = data plane (`{obs_prefix}/exps/exp_XXXX/{spec.json, mixture_data/, result/}` + `assets/` worker bundle, auto-uploaded); job backend = compute (one job per exp, npu_per_job cards, no cross-node collectives); results materialize as LOCAL `exp_XXXX/` dirs byte-identical in shape to local runs → search resume (meta.json exact-weight match) and stage fingerprints need ZERO changes. Pieces: `src/climbmix/pipeline/nanochat_cmds.py` (shared command/env/eval-dir builders extracted from ProxyRunner — single source of truth for local AND remote, stdlib-only so the worker imports it standalone in containers; ProxyRunner now delegates, behavior preserved, all regression suites green); `src/climbmix/remote/` (ExpSpec JSON contract w/ spec_version guard + fully-built torchrun cmds pinned by the submit host; JobAPI/ObsStorage protocols + Mock implementations (the real platform adapter lived here as stubs until the 2026-08-29 split, then moved OUT to a private backend repo — the public core is platform-neutral, wired via backends.py BackendBundle registration)); `scripts/remote_worker.py` (standalone container worker: download shards → symlink base ckpt → mid_train → private-eval-base farm (shared code) → CSV claim → result.json/logs/ckpt upload, crash-safe best-effort result upload); `scripts/dispatch_remote.py` (M3 one-off dispatch + `--check-assets` OBS bootstrap verifier); `runs/run_climbmix.sh` `REMOTE_*` knobs → generated `remote_config.json` → `run_climb.py --remote-config` (RemoteExecutor replaces ProxyRunner; `local_parallel` hybrid fleet — master-node cards join by default: shell default `REMOTE_LOCAL_PARALLEL=1`, first NUM_NPU/NPU_PER_EXP configs local via the parent's parallel path, rest remote, all concurrent, results merged in order; `REMOTE_NPU_PER_JOB` defaults to `NPU_PER_EXP` so k is fleet-wide fixed per docs/parallel_k_selection.md, with idle + k-mismatch guards in both the shell pre-flight and RemoteExecutor init; DYNAMIC SUBMISSION (queue-consumer + live capacity probe — the 98-exp/16-card scenario): the shared pool fluctuates 10-200 cards, so a batch never assumes one fixed-size submission burst. Remote configs go into a QUEUE; up to `max_concurrent_jobs` worker threads take a config only after acquiring an in-flight SLOT, so queued configs are never prepped early (no OBS/disk pileup behind a small pool) and a finished job frees its slot instantly — the next config starts with ZERO backoff delay. The in-flight limit starts from a SYNCHRONOUS capacity probe (`JobAPI.free_job_slots()`, adapter-normalized jobs-not-cards) and a monitor thread re-probes every `poll_interval_s` while the batch runs: pool growth wakes parked workers immediately (new jobs start as idle cards appear), pool shrink queues pickups WITHOUT ever killing in-flight jobs (limit floors at inflight). `free_job_slots()==None` (platform backend, no quota-usage query) degrades cleanly to the fixed limit + submit-rejected backoff: capacity-rejected submits raise `TransientSubmitError` (MockJobAPI injectable; the platform backend maps quota/throttle codes to it, M1) and `_submit_with_retry` backs off exponentially (30s→600s, up to `submit_retry_timeout_s` default 24h, `REMOTE_SUBMIT_RETRY_H`) — the retry net also covers probe/submit races; transient rejections NEVER burn a config. Local prep+upload is semaphore-bounded (`max_prep_parallel` default 4, `REMOTE_MAX_PREP`) so raising `REMOTE_MAX_JOBS` buys in-flight jobs, not prep stampedes; hard submit errors (bad image/auth) are recorded so sibling configs fast-fail without wasted prep. Remote resume mirrors local: L1 meta.json reuse (no job); L2a `.remote_mid_ok` (ckpt on OBS → eval-only job, worker re-downloads ckpt); L2b local marker valid → upload local ckpt → eval-only. Failure semantics identical (per-exp exception → inf/0.0 ProxyResult → NaN in predictor, resume re-runs). Guarded by `/tmp/opencode/test_remote_executor.py` (129 checks: end-to-end sim with REAL worker subprocess + MockJobAPI + filesystem fake OBS; both L2 paths; train-fail/eval-fail/timeout(crash-path result.json recovery exercised); hybrid fleet ordering + guard warnings; parallel batch; weight-mismatch rerun; spec round-trip incl. container paths + env passthrough; fingerprint policy: REMOTE_* knobs excluded, remote code files hashed BOTH; dynamic submission — transient-rejection retry w/o burn, retry-timeout burn, hard-error fast-fail w/o prep, prep concurrency bound; dynamic capacity — 2-slot pool peak==capacity w/ precise admission (zero rejected submits), mid-run pool growth wakes queued workers, pool shrink never kills in-flight, queued configs not prepped early, input-order preserved) + dispatch smoke (real torchrun + real parquet pool through ShardMetadataManager). Fingerprint impact: search 59c9ee9467c0f7d9 / target d4f0c34d4a15779c on the current tree — new code changes BOTH (by design; all code must land before production search starts — done in this changeset; NOTE user's parallel predictor-Spearman/verbatim-M-of-N work is also uncommitted in the same tree and enters the same fingerprints).
- **M1 environment survey (server-side remainder, ~30min)**: the platform adapter (gateway REST client, token/auth provider, OBS storage, config template, hello-world calibration tool, 79-check adapter suite) now lives in the PRIVATE backend repo (clone alongside climbmix; register via `RemoteConfig.backend_module='climbmix_ma:create_backend'` — see docs/remote_setup.md). Security model after the 2026-08-29 two-repo split: public repo = contracts + executor + mock only (zero platform specifics); private repo = platform identity values (endpoint/IDs/pool/image) in its config template; per-user credentials stay in `~/.config/climbmix/remote_ma.json` (gitignored everywhere). Status/error calibration is CONFIG-DRIVEN (`status_map_int`/`status_map_str`/`transient_error_patterns` — no code edits). Remaining on the SERVER: clone both repos + PYTHONPATH → fill the config (auth only) → backend hello-world calibration → big-asset upload to `assets_big/` (`--check-assets` verifies).
- **M3 consistency validation (gate to production)**: ⓪ hello-world job green (the backend repo's calibration script — status maps + transient-error patterns are config keys now); ① short 5-step remote smoke via dispatch_remote; ② exp_0000 weights run BOTH sides into throwaway dirs: mixture shard sha256 byte-identical + Δstem_metric < 0.002; ③ concurrency wave — 2-4 dispatches in flight at once, observe the dynamic scheduler against the real pool (slot handoff on completion with zero backoff, rejection backoff, in-flight peak == intended slots) and let it verify T(4)≈5.3h incidentally (dedicated S(k) benchmark SKIPPED by decision 2026-08-28 — the two-anchor model gets its first real datapoint here; also observes the pool-full error code → finalizes the TransientSubmitError mapping) → `REMOTE_ENABLED=1 bash runs/run_climbmix.sh` (M5). Search at mixed-fleet width ≈1-1.5 days (fleet sizing per `docs/parallel_k_selection.md`); target arms stay local (~12h + eval). M6: report_generator + final report.
- **M4 launch planner: DECIDED NOT to automate (2026-08-28)** — k-selection runs once per production launch with human eyes on the live pool shape; the procedure lives as recommended knobs + the 30-second recalculation method in `docs/remote_setup.md` §M5 (first run: NPU_PER_EXP=4 per λ_max≈5h, REMOTE_MAX_JOBS=⌊A_eff/4⌋≈6-8 with A_eff=pool median~P25, local parallel default on; Wall ≈ 5.3h × 6 waves ≈ 32h for [20,10,5]; second run after first green: λ_max≈10h → k=2, same formulas).
- **K auto/ask gate (DESIGN 2026-09-04, implement post-prod1)** — turn the merge-profile diagnostics into the K decision, agent-style human-in-the-loop: `K_ENHANCED=auto|ask|<int>`. `auto` → `K_final = clamp(elbow, K_min=3, K_max)` and proceed WITHOUT human input only if guards are green: (a) elbow at tree end or max distance jump < ε → degrade to `ask`; (b) largest-cluster share > 90% at the chosen K (prod1 lesson: C0 = 99.86% of docs at K=3) → force `ask`; (c) dust-cluster anomaly → warn; (d) natural_K(τ) swinging an order of magnitude across τ → print "τ unusable as a decision knob on this pool" (prod1: 0.7→49 / 0.8→4 / 0.9→≤3). `ask` → compute the recommendation, write `k_recommendation.json` + a decision-ready console block (elbow value, jump size, cluster-size distribution, paper band 15-30, one-line relaunch command with the pinned K), then EXIT CLEANLY at the gate — stop/ask/resume costs seconds thanks to pool-keyed caches (embeddings + FULL-POOL K_init labels in kmeans_K*.npz survive the restart); deliberately NOT an interactive stdin wait (production runs are nohup/setsid, no tty). `<int>` → pinned, current semantics (prod1_k14 uses 14). Replaces the floor-stop advice line ("stricter τ" — refuted as a blunt knob on skewed pools). Gate skeleton (recommend → guards → auto/ask/stop) generalizes to later human-gated decisions (PRUNE_COLUMN_FLOOR upgrade after eyeballing pruned samples, NPU_PER_EXP k-selection) — K first only.
  - **SUPERSEDED (2026-09-07, prod2 决策)**: A6/A7 证明本池的距离合并树本身退化(912/912 合法、dendrogram 平滑 0.13→0.68、fixed-K 扫描 14/21/24/32 全留 97.5-99.0% 巨簇)——**任何 K 选择都救不了**,gate 设计失效。替代落地:`MERGE_STRATEGY=balanced`(容量约束平衡划分到 K_ENHANCED=15,`balanced_macro_clusters`,paper_deviations.md D14)+ 硬结构闸门 `validate_cluster_structure`(max token share > 50% → abort)。同批 prod2 修复:mmlu acc-only 回退(accuracy-primary 语义恢复)、no-signal 守卫(predictor R²≤0 或全任务 f<0 → 自动选实测最优 config 并标注)、from_dict 自然排序。
- **E. Embed-at-scale（全池嵌入，弹性多节点——一次性 40h 缓存投资的加速项，非首个生产 run 前置）**: 生产走全量嵌入（EMBEDDING_SAMPLE_SIZE=0；抽样模式在 100B 池上 98% 文档退化为 domain 级粗标签，聚类价值崩塌）。**规模定案（2026-08-31，20-分片冒烟实测）**: 全池 = **116M docs / ~93B tokens**（116K docs/分片 × 1000；08-29 的 "11.61M/4.3h" 勘误把 100-分片子集元数据当成了全池，已二次勘误）；流式路径实测 **~800 docs/s**（8 卡均衡，爬坡 ~5 min）→ **单节点 ≈40h**（一次性，缓存与生产共用；周末挂机可行）。E 的弹性多节点把 40h → **40/J h**（4 节点 ~10h），并把该能力沉淀为未来扩池的前置。方案（复用 M2 的 JobAPI/OBS 机器）：work unit = shard（1000 个），dispatcher 按当时空闲节点派发 shard-range 作业（节点内每卡一个 embed worker 进程），作业上传 {该区间嵌入块, 账本} 至 OBS，本地按 shard 位置拼接进 pool cache memmap——resume 语义不变，池波动下进度增量 banked（公共池资源不确定，按可用量伸缩是设计目标）。前置：分片数据 + stella 模型上 OBS；磁盘峰值 ~950 GB fp32（475 npz + 475 tmp，fp16 被否——accuracy first），单节点跑前必须 df 确认。实现项：faiss 全量指派需分块（已落地 `cluster_assign.py` + memmap 路径，116M docs = 238 TFLOP ≈ 15-50 min，非瓶颈）；缓存命中重跑的 np.load 475 GB 进 RAM（1.5 TB 主机可容，npy-memmap 旁车为更优解，另列）
- **target_runner.py d28 安全配置对齐 (2026-08-27, VERIFIED on server 2026-08-28)**: Python target 路径 (`--skip-target` 关闭时) 的 mid_train 命令补齐 shell Step 6 的实证配置 — `--device-batch-size=1` (不传则继承预训练 dbs=32, d28 第一个 forward 即 OOM)、`--loader=flat`、`--sample-every=-1`、`--core-metric-every=-1`; eval 命令不动。缓存防陈旧: pool embedding/kmeans 缓存键加入 `embedding_sample_size` (仅 >0 时, 全池流式键不变), 且全部 4 个缓存命中点加行数守卫 (不匹配 → 警告 + 重算, 30 项测试 `/tmp/opencode/test_hyperparam_speedrun.py`)。2026-08-28 服务器全量重跑确认：eval 修复 (core_bs=8) 通过 Step 7, STEM metric 0.1722, 全流程绿, `mark_completed` 归档为 `speedrun_20260828_120406`; search 指纹 `c29c218fb4ca89c4` 跨 eval 修复不变 (stage-scoped 指纹按设计生效, search 产物保留, Step 1-3 跳过)。`scripts/diagnostics/step7_eval_standalone.sh` (57-check sim) 保留为以后 eval-only OOM 的免重训诊断工具 (本次未启用 — 全量重跑仅 ~20 min)
- **Step-6 interruptible resume**: DECIDED NOT TO BUILD before production (2026-08-28) — target arms are ~6h each, whole-arm retry accepted; revisit only if a production arm actually dies near completion
- **d28 ckpt dtype VERIFIED on server (2026-08-27, check_ckpt_dtype.py)**: H1 confirmed — transformer matrices fp32 (5.25 GiB / 182 tensors) + bf16 embeddings (value_embeds 1.75 + wte 0.12) + fp32 lm_head (0.25) = 7.38 GiB params; optimizer rank0 shard 1.25 GiB mixed (fp32 Muon momentum for matrices, bf16 AdamW moments for embeddings — by design). Per-card static (ws=8) = 7.38 params + ~7.37 grads + 1.25 optim shard = **16.0 GiB**. The linear dbs projection that followed (dbs=1≈18.9 / dbs=2≈21.8) was REFUTED on 2026-08-28: dbs=2 OOM'd in forward at 26.9G and the binding constraint turned out to be the dbs-independent optimizer stacking peak — see round-3 entry above and the corrected check_ckpt_dtype.py. NOTE: `d28_speedrun` is just the Step-6 symlink to d28 (created/cleaned by the runner) — the speedrun has always trained the real d28; the dtype check finding no model files under it is expected
- **Production dbs decision (FINAL)**: dbs≥2 empirically dead (forward wall 26.9-27.5G); the binding constraint is the dbs-INDEPENDENT optimizer stacking peak (~24.2G vs ~24.5G torch ceiling). Production locks `MID_DEVICE_BATCH_SIZE=1` + `--eval-every=-1` (quadmix-proven path, now in speedrun Step 6, run_climbmix Step 6, and target_runner). The speedrun's `Peak memory usage` print is for the record only — it no longer feeds any dbs projection
- Speedrun green + archived (2026-08-28) → RemoteExecutor code complete (see Remaining M2 entry) → remaining path: M1 survey → M3 validation → M5 production `bash runs/run_climbmix.sh` (REMOTE_ENABLED=1)
- Confirm on remote: mid_train val-shard convention (last shard = val, minimum val size)
- Analyze results, write final report
- **At-scale chunked cluster assignment (2026-08-29)**: the cluster stage after full-pool embedding had a memory wall at 116M docs × 1024 dim fp32 (475 GB): `np.isnan(emb)`/`emb == 0` full-matrix bools (~119 GB each), `np.nan_to_num` full copy (~475 GB) — fine at speedrun scale, OOM at production. Server has 1.5 TB RAM (current scale OK per user), but the wall is real at ~400M docs or under container cgroup caps; fixed with a no-regret change, accuracy-first: new `src/climbmix/core/cluster_assign.py` provides chunked prescan (NaN/Inf/zero-mask, elementwise-equal semantics incl. the mixed-row `[1.0, NaN]` subtlety), chunked `assign_in_chunks` (row-independent exact argmax → labels ELEMENTWISE IDENTICAL to the single-shot form, asserted exact in tests), chunked side-car sanitize for the rare NaN case (rare path: full-disk copy, bounded RAM; removed after), and cgroup-aware chunk sizing: budget = min(MemAvailable, cgroup v2 `memory.max`/v1 `limit_in_bytes`)/8 clamped [64 MB, 8 GB] — `/proc/meminfo` reports HOST memory, wrong inside containers; >8 GB buys nothing (total I/O/flops unchanged, faiss saturates at million-row batches); `CLIMB_ASSIGN_CHUNK_GB` overrides. `cluster_embeddings_faiss` routes: memmap input (fresh full-pool run — `embed_texts_streaming` returns the memmap) → chunked path; ndarray (npz cache hit / subsample) → original single-shot code unchanged (byte-identical behavior); `kmeans.train` unchanged (faiss internally subsamples ≤256 pts/centroid, never materializes the matrix). Also `runs/run_climbmix.sh` Steps 1-3 now `2>&1 | tee search.log` (same as speedrun; pipefail already set) so production search console checkpoints (early-stop, funnel, online ρ) land on disk. Verified: `/tmp/opencode/test_cluster_assign.py` 27 checks — sizing (fraction/clamps/cgroup-min/env override), exact-equality chunked vs single-shot on a 50K×64 memmap with NaN/mixed/zero rows, zero-mask semantic preservation, fake-faiss wiring (memmap trains on memmap no-copy, ndarray unchanged, NaN side-car cleaned up), RLIMIT_AS=2.5 GB demo on a 1.6 GB memmap (chunked completes with all 25M rows, full materialization MemoryErrors — wall is real), tee wiring. Regression: embed_fanout 17, cluster_guard, two_pass, stage_fingerprint, search_verbatim, json_finite — green. Fingerprint: cluster_assign.py is new → both fingerprints shift (folds into the planned rerun). fp32 kept everywhere (fp16 was rejected — accuracy first); OBS-for-disk noted in docs/embedding_performance.md §7 when the ~950 GB memmap+npz coexistence window outgrows the data disk.
- **smoke_search: production-geometry search simulation + feature-importance report section (2026-08-29)**: closes the speedrun's open question (held-out ρ=nan at N=18) with a zero-NPU proof. `scripts/smoke_search.py` replaces proxy training with a synthetic ground-truth utility (quality-aligned optimum u(w)=q·w+λ(1−‖w−w*‖²), w*=softmax(q); per-task acc/NLL observations with per-config_id-seeded noise → resume-stable) and runs the REAL IterativeBootstrapper loop under both geometries: main 20,10,5 (N=35, production) and paper 64,32,16 (N=112, paper §"total of 112"). SMOKE OK 15/15: main val ρ finite (0.674 — nan was indeed a small-N artifact), paper ρ 0.735 ≥ main (D.10 trend), online backtest positive at paper N, guided funnel's best TRAINED config ≥ p99 of a 10K random pool (paper: 1.009 vs 0.928; main ≥ p95: 0.852 vs 0.827), pruning funnel sane, state strictly jq-parseable + reloads, feature importances exposed. Verdicts gate machinery health only — runtime ~40s CPU. **Finding (reported, not gated): final full-design-space selection leaves value on the table** — the predictor-argmin over the 105K-candidate pool picks a config WORSE than the best already-trained one (paper: 0.723 vs 1.009; main: 0.657 vs 0.852; gap ≈ +0.2-0.3 utility). Mechanism: trees don't extrapolate; training distribution narrows to the guided band, wide-pool corners get leaf values from that band, and argmin exploits the error (optimizer's curse). Paper-faithful semantics (§2.2 predictor-argmin from design space) — **decision 2026-08-29: keep paper semantics** (D3 note in docs/paper_deviations.md); the "best of {predictor-argmin, best measured}" variant and the "+1 proxy train of the argmin pick" variant were both rejected — the latter serializes ~10h at the tail of a parallel run (one straggler experiment owns the whole card group). Also: report_generator gained a "Predictor Feature Importance" section (split counts + share of the final LightGBM, sorted desc; renders only when in-memory iter_results carry a fitted model — fresh report or resume-refit; silently skipped on stale-K/no-model). Tests: test_smoke_search 18 checks (source, full e2e both geometries, verdict x2 regex, gap reported; report section render/sort/placement/absent/K-mismatch) + reran p1_stem_ratio_report, json_finite, p0, search_verbatim — green.
- **search_state.json must be valid RFC-8259 JSON + Steps 1-3 console log (2026-08-29)**: user hit "not valid json" on jq — root cause: small-N degenerate Spearman is nan (constant predictions → zero rank variance), and `json.dump`'s default happily writes literal `NaN`, which Python's `json.load` reads back (resume worked) but jq/strict parsers reject. Fixed at three layers: (1) source — `iterative_bootstrapper` stores val_r2/val_spearman as None when non-finite and filters val_preds/val_targets to finite pairs (mirrors the existing online-eval None convention; report already renders None as N/A); (2) write boundary — `io_utils.atomic_write_json` recursively maps non-finite floats → null for every JSON state file (also covers the rare diverged-run nan loss in accumulated_scores); (3) read boundary — `_load_state` restores None → float('nan') for accumulated_scores, and for accumulated_per_benchmark preserves the (acc-dict, nll-dict) tuple shape: whole-value None = unmeasured (predates the JSON change), null INSIDE a per-task dict = sanitized non-finite → back to nan (isfinite filters it); first restore attempt wrongly float()-ed the dicts — caught by test_p0's resume check. Existing server files with NaN literals stay loadable (Python json.load is tolerant) — optional one-shot repair: load with json.loads, walk nan→None, dump back. Also: speedrun Steps 1-3 (`run_climb.py`) now `2>&1 | tee "$OUTPUT_DIR/search.log"` (pipefail was already set, so failures still propagate; the `[ -f sampled_dataset.parquet ]` hard gate unchanged) — predictor/early-stop/funnel console checkpoints are now on disk, not just ephemeral terminal output. D4 note added to docs/paper_deviations.md explaining why small-N held-out Spearman is necessarily nan (leaf-collapse, not a bug; paper's 94% is at N=112). Tests: test_json_finite (10 checks: strict-parse round-trip incl. nan/inf/nested/np.float64, load-state None→nan restore) + reran p0 17, search_verbatim, fixes, hyperparam_speedrun 48.
- **Multi-NPU embed fan-out for the subsample path (2026-08-28)**: user observed speedrun embedding on a single card — root cause: the discovery SUBSAMPLE path (EMBEDDING_SAMPLE_SIZE > 0) loads the sample as a texts list and embeds via `embed_documents` (one device), while only the full-pool streaming path had the 8-card process fan-out. Fix: `embed_documents` now fans large in-memory lists out across all visible NPUs, mirroring `embed_texts_streaming`'s proven pattern exactly (parent dummy-loads the model for emb_dim, releases + flushes device cache, one spawn worker per NPU with `ASCEND_RT_VISIBLE_DEVICES=worker_id`, contiguous chunks into a shared memmap, 15s progress monitor, aggregate → normal embedding cache). Gated on `n_npus > 1 and n >= _MULTI_NPU_MIN_DOCS (4096)` — below that the single-device path wins (per-worker model load ~30-40s dominates; breakeven ≈4.3K docs at ~94 docs/s/card); any fan-out failure falls back to single-device. 20K speedrun sample: ~4 min → ~1.5 min; prune_report-style 100K samples: ~18 min → ~2.5 min. Production full-pool (sample=0) already streamed multi-card, unchanged. Note: if the 20K embed already completed for the current pool key, the pool-keyed cache hits and the fan-out won't re-trigger for THAT key (by design — fingerprint changes don't invalidate the embedding cache); it pays off on the next pool change / fresh embed. Tests: `/tmp/opencode/test_embed_fanout.py` (17 checks: source hook/threshold/fallback, `_fanout_bounds` purity, e2e spawn with a fake-worker module — correct row offsets, blank→'empty' substitution, cache write, memmap cleanup, failure propagation with no cache + cleanup; script is `__main__`-guarded because spawn re-imports `__main__`). embedding_cluster.py is SEARCH_ONLY → search fingerprint changes (folds into the single post-refactor rerun). All 14 suites + fan-out suite green.
- **Speedrun pool 10×100K shards (2026-08-28)**: Step 0 no longer copies the first 5 `part-*.parquet` verbatim (row count uncontrolled) — it now streams `DATA_DIR` parquet in 20K batches and writes exactly `SPEED_SHARDS`×`SPEED_SHARD_DOCS` docs (default 10×100K = 1M, env-overridable), splicing seamlessly across source-file boundaries, memory-bounded at ~one shard, hard-failing with available/required counts if the pool is short. `.spec` marker in `$SPEED_DATA` prevents a stale pool of a different shape from being reused; pool shape is fingerprinted (`speed_shards`/`speed_shard_docs` in FP_SEARCH_PARAMS → search mismatch → stage_gate archives the WHOLE dir, correct since everything downstream depends on the data shape); pool-keyed embedding cache re-keys automatically (one 20K re-embed). Tests: `/tmp/opencode/test_hyperparam_speedrun.py` +11 incl. functional (heredoc extracted from the shipped .sh and executed: exact shard sizes, row order across boundaries, no over-write, insufficient-pool error). Note: 1M docs ≈ several GB in `/tmp/speedrun_data` — check server disk before launch.
- **Speedrun configs default 6,6,6 (2026-08-28)**: `runs/speedrun_climbmix.sh` `CONFIGS_PER_ITER` now env-overridable (was hardcoded `2,3,2`), default `6,6,6` (=18 ≥ 10) so the single post-RemoteExecutor rerun also exercises the N≥10 paths — val split + `lgb.early_stopping` + guided sampling + pruning funnel + online backtest — i.e. everything production [20,10,5] runs, at speedrun cost (proxy trainings 7→18, Steps 1-3 wall ×~2.5, same-day). Fastest smoke still available via `CONFIGS_PER_ITER=2,3,2`. Note `configs_per_iter` is IN `FP_SEARCH_PARAMS` → fingerprint/ archive as intended (the rerun is wanted anyway). D4 doc: colsample_bytree formula noted as dormant-by-design (clamps to 1.0 at K_final≤15). Tests: `/tmp/opencode/test_hyperparam_speedrun.py` (+4 checks)
- **Predictor/pruning visibility batch (2026-08-28)**: mid-run console + report.md now show LightGBM fit/predict/prune status end-to-end. Mid-run, every guided round prints a pruning line (pool → novel → top-N % → M verbatim; below-cutoff count never trained), the top-N predicted-target band, an honest **online backtest** (Spearman between predictions recorded BEFORE training that round's configs and their actual scores — needs no N≥10 split, so speedrun-sized runs get a predictor-quality signal too; degenerate constant predictions → None, never fabricated), and the fit prints early-stop round (`best_iteration=k/500`, or "no early stopping"). report.md gains a "Predictor Quality (D.10)" section (per-round val R²/ρ + online ρ table, pooled held-out Spearman, Fig.9-style `predictor_scatter.png` in score space with a perfect-prediction diagonal) and a "Search Pruning" section (per-round pool/novel/top-N/sampled/excluded table + totals; iter-1 Dirichlet note). Everything persists to search_state.json (`online_eval`, `pruning_history`; tolerant `.get()` so old files resume unchanged) and re-attaches into reconstructed iteration results after a resume — a crashed run's report is as complete as a fresh one's. `scripts/smoke_predictor.py` (no NPU, seconds) exercises the one production path speedrun structurally cannot cover (N≥10 → val split → `lgb.early_stopping` callback → val ρ; lightgbm-version dependent) — run it once on any new machine before a large-scale search. Expectation management: with M=10 and a wide predictor tie-group, online ρ is rank-noise-dominated — a monitoring signal, not a gate (measured: pool-wide ρ 0.95 / band ρ 0.68 while a 6-point draw read 0.19). Tests: `/tmp/opencode/test_search_verbatim.py` (91 checks: +pruning record, +online backtest incl. honest-degenerate None, +state persistence + backward compat, +resume re-attach, +report sections/scatter/fallback)
- **Search paper-fidelity batch (2026-08-28)**: guided sampling returned to paper §2.2 verbatim semantics — iter-2+ now draws M configs without replacement from the predictor-ranked top-N (N=3×sample_from_top_m=96), no Dirichlet perturbation. Rationale (user decision after audit): the old Dir(5·w) perturbation was K-blind — at K=15 its mean L1 offset (1.07) exceeded the pool's own pairwise spread (0.97), diluting the ranking signal instead of adding exploration; paper's exploration comes from the top-N bandwidth + pool randomness, no extra knob. `sample_from_top_n` kept for final-selection refine only (D3). Predictor logging: held-out val Spearman (paper D.10 metric, numpy average-rank impl verified against scipy incl. ties) + val R² + (pred, actual) pairs per iteration persisted to search_state.json (`predictor_eval`, requires accumulated N≥10 — speedrun N=7 never has one, production 35 gets one per iteration); misleading train-set R² print relabeled. Code change → search fingerprint changes → server speedrun re-runs Steps 1-3 next launch (embedding cache moves with the archive dir, minutes). Deviation D2 flipped to "returned to paper" with the numeric evidence; positive-audit section now records the paper's "pruning" = selection-level narrowing (not mid-training kills) and the D.10 Spearman caliber. Tests: `/tmp/opencode/test_search_verbatim.py`

## Known Limitations

- Resume is atomic per experiment/training run, NOT per training step: an interrupted nanochat training restarts from step 0 (partial target checkpoints are deliberately cleared before retrain)
- Fingerprint does not detect: edits inside nanochat-npu, or data files whose names/row-counts are unchanged (content swaps)
- Embedding cache staleness: pool cache keys cover (shard manifest, model, truncate len, sample-size-if-subsampled) + row-count guards on load, but a same-size content swap inside a shard still hits the old cache (same limitation as fingerprints, content-keyed by size only)
- Predictor refit on resume assumes LightGBM determinism (same accumulated data → same model); verified identical weights in local integration tests
- quality_cluster discovery breaks when K_init > number of domains (centroids shape error) — unused in production (embedding_cluster)
- NPU embedding compatibility relies on the stella buffer repair; re-verify NaN=0 after any sentence-transformers upgrade
- ClimbMix shard count is adaptive (3-50), not full 400B dataset — only enough to match 30% of STEM doc count
- d28 DEPTH_INFO requires meta_*.json checkpoint for auto-detection; without checkpoint, raises ValueError
- STEM data fact density D unknown — cannot determine if subdisciplines cross phase transition threshold
- Intentional deviations from paper documented in docs/paper_deviations.md (12 items: budget/sampling/selection/LightGBM/cluster-band/τ/random-shortfall/metric/70-30-mix/scale/chars-4/eval-cap) + docs/scoring_metric_design.md (SNR score, 70/30 anti-forgetting mix, chars/4 token estimation)
